---
phase: 03-scene-themes
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - lib/scenes/walk/walk_painter.dart
  - lib/scenes/walk/walk_scene.dart
  - lib/scenes/car/car_painter.dart
  - lib/scenes/car/car_scene.dart
  - lib/scenes/scene_registry.dart
  - test/scenes/walk/walk_painter_test.dart
  - test/scenes/walk/walk_scene_test.dart
  - test/scenes/car/car_painter_test.dart
  - test/scenes/car/car_scene_test.dart
autonomous: true
requirements: [SCENE-03, SCENE-04]
must_haves:
  truths:
    - "Selecting Walking Home shows a character walking a path toward a house, arriving at the door exactly at time-up (left 6%→68%)."
    - "Selecting Car on a Road shows a car driving toward a house/destination, arriving at time-up on the same 6%→68% mechanic."
    - "The character bobs (0.62s loop) and the car's wheels spin (0.7s loop), both driven by the same per-scene ticker; both freeze in place when the timer is not running."
    - "After this plan every SceneTheme maps to a real scene; the interim pending fallback is removed and sceneFor is exhaustive over all four themes."
  artifacts:
    - lib/scenes/walk/walk_painter.dart
    - lib/scenes/walk/walk_scene.dart
    - lib/scenes/car/car_painter.dart
    - lib/scenes/car/car_scene.dart
  key_links:
    - "arrivalLeftFraction(p) = (6 + p.clamp(0,1)*62)/100 lives in walk_painter.dart and is reused by car_painter.dart — one shared arrival formula for both scenes."
    - "scene_registry.sceneFor maps walk→WalkScene and car→CarScene, replacing the Plan 01 pending fallback and making the switch exhaustive."
---

<objective>
Deliver the final two scenes — Walking Home and Car on a Road — which share the arrival mechanic (distance-remaining tracks time-remaining), and finish the scene registry so all four themes render their real scenes with no pending fallback left.

Purpose: Complete SCENE-03 and SCENE-04 and close the phase's scene set; both scenes use the same arrivalLeftFraction so distance visibly maps to time.
Output: WalkPainter (+ arrivalLeftFraction pure fn + bob loop) + WalkScene; CarPainter (reusing arrivalLeftFraction + spin loop) + CarScene; exhaustive scene_registry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scene-themes/03-CONTEXT.md
@.planning/phases/03-scene-themes/03-RESEARCH.md
@.planning/phases/03-scene-themes/03-PATTERNS.md
@.planning/phases/03-scene-themes/03-UI-SPEC.md
@lib/scenes/scene_renderer.dart
@lib/scenes/scene_registry.dart
</context>

## Phase Goal

**As a** young child waiting for a timer, **I want to** watch a full-screen wordless scene that visibly changes as time passes, **so that** I can tell from across the room roughly how much longer I have to wait — without reading any numbers or words.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Walking Home end-to-end (arrival fn + painter + scene + registry)</name>
  <files>lib/scenes/walk/walk_painter.dart, lib/scenes/walk/walk_scene.dart, lib/scenes/scene_registry.dart, test/scenes/walk/walk_painter_test.dart, test/scenes/walk/walk_scene_test.dart</files>
  <read_first>
    - .planning/phases/03-scene-themes/03-RESEARCH.md (Code Examples §"Arrival mechanic"; Pattern 2 bob loop; Pitfall 5 static-Paint caching)
    - .planning/phases/03-scene-themes/03-UI-SPEC.md (§"E. Walking Home" Color + Geometry tables; §"Arrival Mechanic"; Decorative Loop Contract bob row; End-of-run visual correctness for walk)
    - .planning/phases/03-scene-themes/03-PATTERNS.md (walk_painter.dart + scene pattern assignments)
    - lib/scenes/scene_preview.dart (WalkPreviewPainter: sky gradient, ground band, house + character drawing precedent, colors)
    - lib/scenes/scene_renderer.dart (SceneRendererState: progress getter, loopPhase for bob)
    - lib/scenes/disc/disc_scene.dart (scene widget shape to mirror)
    - lib/scenes/scene_registry.dart (current switch: walk still on pending fallback)
    - test/support/progress_sweep.dart
  </read_first>
  <behavior>
    - arrivalLeftFraction(0.0) == 0.06; arrivalLeftFraction(1.0) == 0.68; arrivalLeftFraction re-clamps out-of-range input (e.g. arrivalLeftFraction(1.5) == 0.68, arrivalLeftFraction(-0.2) == 0.06).
    - WalkScene pumped across progress 0.0→1.0 renders without throwing at every sample.
    - A mounted WalkScene subtree has no Text/numerals and no gesture-reactive ancestor.
    - WalkPainter.shouldRepaint is true when progress or bob phase changed, false otherwise.
  </behavior>
  <action>
    Create walk_painter.dart with the shared top-level pure function `double arrivalLeftFraction(double progress) => (6 + progress.clamp(0.0,1.0) * 62) / 100;` (locked D-01, defensive re-clamp per RESEARCH). Create `WalkPainter extends CustomPainter` taking `progress` and a `bobPhase` (0..1) as fields. In paint, per §E (px are logical px; % are fractions of size computed at paint time; combined values like `bottom: 20% + 6px` = size.height*0.20 + 6):
    - Sky: full-bleed 3-stop top→bottom LinearGradient Color(0xFFBFE0EE) 0% → Color(0xFFDCEAEF) 45% → Color(0xFFE9F2E0) 63% (static — cache the shader/Paint per Pitfall 5).
    - Clouds ×2 (decorative, static): 70×26 r20 at left 40, top 80; 54×20 r16 at right 50, top 150; colors Color.fromRGBO(255,255,255,0.7) and 0.6.
    - Ground: full width, height 34% of screen, bottom 0, color Color(0xFFCFE0A8).
    - Path (dirt band): full width, height 22px, bottom 20% of screen, color Color(0xFFE4D3A6).
    - House group (130px wide) anchored right 26, bottom 20%+6: roof triangle base 140 (70 each side) height 48 Color(0xFFC98A5E); body 112×92 r10 Color(0xFFEAD7B8) (−2px roof overlap); door 36×52 top-corners r10 Color(0xFFB5794E) bottom-centered; window 22×22 r5 at top16/left16 within body Color(0xFFF6D9A8).
    - Character group at bottom 20%+10, horizontal center x = size.width * arrivalLeftFraction(progress), with the bob decorative loop applied as a vertical translateY of 0 → −7px → 0 over the 0.62s loop (derive from bobPhase, ease-in-out): head 38×38 circle Color(0xFFF0C9A0); two 5×5 eyes Color(0xFF4B4038) at top14, left9/right9 within head; body 50×52 top-corners r24 bottom-corners r16 Color(0xFFE0805F). (The #E0805F character body is the accepted destructive-color override per STATE.md Phase 2 log — not the reserved affordance color.)
    Cache progress-independent Paints as fields; shouldRepaint compares progress and bobPhase (Pitfall 2). No CSS ease/transition layer.

    Create walk_scene.dart mirroring DiscScene: `WalkScene extends SceneRenderer`, state extends `SceneRendererState<WalkScene>`, build returns full-bleed CustomPaint(painter: WalkPainter(progress: progress, bobPhase: loopPhase(const Duration(milliseconds: 620)))). No Scaffold/GestureDetector/Text.

    In scene_registry.dart, replace the walk pending arm so sceneFor returns WalkScene() for SceneTheme.walk (leave car on pending until Task 2). Import walk_scene.dart.

    Write walk_painter_test.dart (arrival values + re-clamp + shouldRepaint) and walk_scene_test.dart (0..1 sweep renders, no text/gesture) using the progress_sweep helper.
  </action>
  <verify>
    <automated>flutter test test/scenes/walk/</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/scenes/walk/walk_painter_test.dart` passes: arrivalLeftFraction(0.0)==0.06, (1.0)==0.68, and re-clamps out-of-range input.
    - `flutter test test/scenes/walk/walk_scene_test.dart` passes: full 0.0→1.0 sweep renders without throwing; subtree has zero Text and zero gesture-reactive ancestors.
    - `flutter analyze` reports no new errors in lib/scenes/walk/.
  </acceptance_criteria>
  <done>Selecting Walking Home and pressing Start shows the character walking from 6% toward the house door at 68%, arriving at time-up, with a gentle bob; distance-remaining tracks time-remaining exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Car on a Road end-to-end + finalize exhaustive registry</name>
  <files>lib/scenes/car/car_painter.dart, lib/scenes/car/car_scene.dart, lib/scenes/scene_registry.dart, test/scenes/car/car_painter_test.dart, test/scenes/car/car_scene_test.dart</files>
  <read_first>
    - .planning/phases/03-scene-themes/03-UI-SPEC.md (§"F. Car on a Road" Color + Geometry tables; §"Arrival Mechanic"; Decorative Loop Contract spin row; End-of-run visual correctness for car incl. wheels frozen)
    - .planning/phases/03-scene-themes/03-RESEARCH.md (arrival mechanic shared with walk; Pattern 2 spin loop)
    - lib/scenes/walk/walk_painter.dart (import arrivalLeftFraction; house-drawing approach to mirror)
    - lib/scenes/scene_preview.dart (CarPreviewPainter: road, dashed center line, car body/window/wheels colors)
    - lib/scenes/scene_renderer.dart (loopPhase for spin)
    - lib/scenes/car/../disc/disc_scene.dart (scene widget shape)
    - lib/scenes/scene_registry.dart (current switch: car still on pending; walk now real from Task 1)
    - test/support/progress_sweep.dart
  </read_first>
  <behavior>
    - car_painter reuses arrivalLeftFraction from walk_painter (imported, not re-defined): a test asserts car's car-x at progress 0.0 and 1.0 corresponds to 0.06 and 0.68 fractions.
    - CarScene pumped across progress 0.0→1.0 renders without throwing at every sample.
    - A mounted CarScene subtree has no Text/numerals and no gesture-reactive ancestor.
    - After this task sceneFor returns real scenes for all four SceneTheme values (disc→DiscScene, sunrise→SunriseScene, walk→WalkScene, car→CarScene) and the pending fallback is gone.
  </behavior>
  <action>
    Create car_painter.dart. Import `arrivalLeftFraction` from walk_painter.dart (do NOT redefine it — one shared arrival formula, per RESEARCH). Create `CarPainter extends CustomPainter` taking `progress` and a `spinAngle` (radians) as fields. In paint, per §F:
    - Sky: full-bleed 3-stop LinearGradient Color(0xFFF6D9A8) 0% → Color(0xFFF5E4C4) 55% → Color(0xFFEFDCBC) 66% (static, cached).
    - Road: full width, height 32% of screen, bottom 0, color Color(0xFF5A5048).
    - Dashed center line: full width, height 6px, dash 26 on / 26 off, at bottom 16% − 3px, color Color(0xFFE9D9B8).
    - House group (130px wide, no window — body + door only) anchored right 26, bottom 32% − 2px: roof triangle base 140 height 48 Color(0xFFC98A5E); body 112×88 r10 Color(0xFFEAD7B8); door 36×50 Color(0xFFB5794E) bottom-centered.
    - Car group at bottom 16%+6, horizontal center x = size.width * arrivalLeftFraction(progress): body 110×42 top-corners r18 bottom-corners r12 Color(0xFFDE6A4B) with shadow blur (offset (0,4), blur 10, Color.fromRGBO(0,0,0,0.15)); window 56×26 top-corners r14 at top −20 left 24 within body Color(0xFFF0B49B); headlight 8×8 circle at top14 right12 Color(0xFFFFE9AE); two wheels 28×28 with a 6px tire ring at left16 and left66, bottom −11 — rim Color(0xFF3A3230), tire ring Color(0xFF6B5E58); each wheel rotates continuously by spinAngle (0°→360° over 0.7s, linear).
    Cache progress-independent Paints as fields; shouldRepaint compares progress and spinAngle (Pitfall 2). No CSS ease/transition layer.

    Create car_scene.dart mirroring DiscScene: `CarScene extends SceneRenderer`, state extends `SceneRendererState<CarScene>`, build returns full-bleed CustomPaint(painter: CarPainter(progress: progress, spinAngle: 2*pi*loopPhase(const Duration(milliseconds: 700)))). No Scaffold/GestureDetector/Text.

    In scene_registry.dart, replace the car pending arm so sceneFor returns CarScene() for SceneTheme.car, then remove the `_PendingScene` fallback entirely and make the switch exhaustive over all four SceneTheme values (all four now return their real scene). Import car_scene.dart; drop any now-unused pending import.

    Write car_painter_test.dart (shared-arrival assertion + shouldRepaint) and car_scene_test.dart (0..1 sweep renders, no text/gesture); update scene_registry_test.dart to assert all four themes map to their real scene classes and no pending fallback remains.
  </action>
  <verify>
    <automated>flutter test test/scenes/car/ test/scenes/scene_registry_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/scenes/car/car_painter_test.dart` passes: car uses the shared arrivalLeftFraction (0.06 at p=0, 0.68 at p=1).
    - `flutter test test/scenes/car/car_scene_test.dart` passes: full 0.0→1.0 sweep renders without throwing; subtree has zero Text and zero gesture-reactive ancestors.
    - `flutter test test/scenes/scene_registry_test.dart` passes: sceneFor returns DiscScene/SunriseScene/WalkScene/CarScene for the four themes; `_PendingScene` no longer exists.
    - `flutter test` (full suite) is green.
    - `flutter analyze` reports no new errors and no unused-import/dead-code warnings from the removed fallback.
  </acceptance_criteria>
  <done>Selecting Car on a Road shows a car driving from 6% to the house at 68% arriving at time-up with spinning wheels; all four themes now render their real scenes and the interim fallback is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TimerController.progress → arrivalLeftFraction / draw math | Progress feeds position math; the sole input crossing into draw code. Already clamped upstream, re-clamped in the pure fn. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Denial of Service | arrivalLeftFraction / car+walk position math | low | mitigate | arrivalLeftFraction re-clamps progress to 0..1; position offsets are bounded fractions of size, so no negative/NaN geometry can crash the render loop. Guarded by the 0..1 sweep widget tests. |
| T-03-06 | Tampering | scene_registry exhaustive switch | low | accept | Fixed enum→widget mapping with no external input; making the switch exhaustive removes the fallback branch. No further mitigation. |

No high-severity threats. Zero network/auth/persistence/new-package surface (RESEARCH Security Domain).
</threat_model>

<verification>
- `flutter test test/scenes/walk/ test/scenes/car/ test/scenes/scene_registry_test.dart` green.
- `flutter test` full suite green.
- `flutter analyze` no new errors, no dead-code/unused-import warnings after removing the pending fallback.
- End-of-phase human check (D-03): character walk/bob and car drive/wheel-spin animate smoothly with no jank on a low/mid-end Android device (API 24–28), or a throttled emulator as a documented lower-confidence fallback. Recorded as the phase-gate UAT item (SCENE-05 perceptual criterion). Also confirm the STATE.md Phase 3 blocker (real device availability) at the gate.
</verification>

<success_criteria>
- Walking Home renders full-screen; character walks toward the house, arriving at time-up (SCENE-03).
- Car on a Road renders full-screen; car drives toward the destination, arriving at time-up (SCENE-04).
- Both scenes read only progress via the shared SceneRenderer contract; bob and spin loops run and freeze when not running.
- All four themes map to real scenes; nothing is tappable inside any scene (SCENE-05).
</success_criteria>

## Artifacts this phase produces (Plan 03)

New symbols/files created by this plan:
- `lib/scenes/walk/walk_painter.dart`: top-level `double arrivalLeftFraction(double progress)` (shared with car); `WalkPainter extends CustomPainter`.
- `lib/scenes/walk/walk_scene.dart`: `WalkScene extends SceneRenderer`.
- `lib/scenes/car/car_painter.dart`: `CarPainter extends CustomPainter` (imports arrivalLeftFraction from walk_painter).
- `lib/scenes/car/car_scene.dart`: `CarScene extends SceneRenderer`.
- Modified: `lib/scenes/scene_registry.dart` (walk→WalkScene, car→CarScene; `_PendingScene` removed; switch now exhaustive over all four SceneTheme values).

<output>
Create `.planning/phases/03-scene-themes/03-03-SUMMARY.md` when done.
</output>

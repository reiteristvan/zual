---
phase: 03-scene-themes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scenes/scene_renderer.dart
  - lib/scenes/scene_registry.dart
  - lib/scenes/disc/disc_painter.dart
  - lib/scenes/disc/disc_scene.dart
  - lib/screens/running_screen.dart
  - lib/screens/setup_screen.dart
  - test/support/progress_sweep.dart
  - test/scenes/scene_renderer_test.dart
  - test/scenes/scene_registry_test.dart
  - test/scenes/disc/disc_painter_test.dart
  - test/scenes/disc/disc_scene_test.dart
autonomous: true
requirements: [SCENE-01, SCENE-05]
must_haves:
  truths:
    - "Selecting the Shrinking Disc theme and pressing Start shows a full-screen disc that shrinks and shifts green→yellow→red as time remaining decreases."
    - "The disc scene renders with zero text/numerals and adds no gesture handler of its own (SCENE-05)."
    - "Disc motion is driven by a per-scene Ticker polling TimerController.progress fresh each frame, not by the 200ms notifyListeners cadence."
    - "At progress=1 the disc is scaled to its 0.001 floor and colored pure red (#DE6A4B); the scene freezes when phase leaves running."
  artifacts:
    - lib/scenes/scene_renderer.dart
    - lib/scenes/scene_registry.dart
    - lib/scenes/disc/disc_painter.dart
    - lib/scenes/disc/disc_scene.dart
    - lib/screens/running_screen.dart
    - test/support/progress_sweep.dart
  key_links:
    - "setup_screen._handleStart navigates to RunningScreen(theme: _theme) instead of the const PlaceholderRunningScreen()."
    - "RunningScreen body hosts sceneFor(theme) from scene_registry.dart; back-nav + auto-pop-on-done stay in the RunningScreen composition root, never in a scene."
    - "DiscScene extends SceneRendererState and reads progress via the shared ticker; DiscPainter reads only discColorForRemaining(remaining) + drawn radius."
---

<objective>
Build the scene-rendering spine (SceneRenderer contract + per-scene ticker mixin, scene_registry, RunningScreen replacing the Phase 2 placeholder) and deliver the Shrinking Disc scene fully animated end-to-end, so a parent selecting "Shrinking disc" on Setup and pressing Start watches the real progress-driven disc.

Purpose: Prove the whole scene pipeline (Setup → RunningScreen → registry → scene → ticker-polled progress → CustomPainter) with the hero scene, establishing the exact contract the other three scenes plug into.
Output: SceneRenderer/SceneRendererState, scene_registry (disc real; sunrise/walk/car map to an interim pending fallback replaced in Plans 02–03), DiscPainter + DiscScene, RunningScreen, and the shared progress-sweep test helper.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scene-themes/03-CONTEXT.md
@.planning/phases/03-scene-themes/03-RESEARCH.md
@.planning/phases/03-scene-themes/03-PATTERNS.md
@.planning/phases/03-scene-themes/03-UI-SPEC.md
</context>

## Phase Goal

**As a** young child waiting for a timer, **I want to** watch a full-screen wordless scene that visibly changes as time passes, **so that** I can tell from across the room roughly how much longer I have to wait — without reading any numbers or words.

(Derived from the project core value in PROJECT.md/CLAUDE.md; the ROADMAP Phase 3 goal is outcome-shaped and maps directly to this story.)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scene contract + per-scene ticker mixin + Shrinking Disc renderer</name>
  <files>lib/scenes/scene_renderer.dart, lib/scenes/disc/disc_painter.dart, lib/scenes/disc/disc_scene.dart, test/support/progress_sweep.dart, test/scenes/scene_renderer_test.dart, test/scenes/disc/disc_painter_test.dart, test/scenes/disc/disc_scene_test.dart</files>
  <read_first>
    - .planning/phases/03-scene-themes/03-RESEARCH.md (Pattern 1 SceneRendererState sketch lines ~244-298; Pattern 3 discColorForRemaining; Pitfalls 1, 2, 4)
    - .planning/phases/03-scene-themes/03-UI-SPEC.md (§"C. Shrinking Disc" geometry table; Color table; Decorative Loop Contract freeze rule; Interaction Contract)
    - .planning/phases/03-scene-themes/03-PATTERNS.md (scene_renderer.dart and disc_painter.dart pattern assignments)
    - lib/scenes/scene_preview.dart (ScenePreviewPainter abstraction discipline; DiscPreviewPainter colors; withValues(alpha:) convention; explicit shouldRepaint)
    - lib/timer/timer_controller.dart (progress pure getter, phase getter, 200ms _tickInterval)
    - lib/timer/timer_phase.dart (TimerPhase enum)
    - lib/screens/placeholder_running_screen.dart (context.watch phase pattern, provider usage)
  </read_first>
  <behavior>
    - discColorForRemaining(0.6) == const Color(0xFF7FA87A) (pure green, remaining > 0.5).
    - discColorForRemaining(0.5) == Color.lerp(Color(0xFFE8B75A), Color(0xFF7FA87A), 1.0) i.e. pure green at the 0.5 boundary; discColorForRemaining(0.35) is a yellow→green lerp with t = (0.35-0.2)/0.3; discColorForRemaining(0.2) == const Color(0xFFDE6A4B) (pure red at the boundary); discColorForRemaining(0.0) == const Color(0xFFDE6A4B).
    - DiscScene pumped across progress 0.0→1.0 (via the progress_sweep helper) renders without throwing at every sample, including at exactly 1.0.
    - A mounted DiscScene subtree contains no GestureDetector/InkWell/gesture-reactive ancestor and no visible Text/numerals.
    - DiscPainter.shouldRepaint returns true when progress OR decorative loop phase changed, false when neither changed.
  </behavior>
  <action>
    Create `SceneRenderer` (abstract StatefulWidget) and `SceneRendererState<T extends SceneRenderer>` (with TickerProviderStateMixin) in scene_renderer.dart, implementing RESEARCH Pattern 1 verbatim: one Ticker created in initState, started on TimerPhase.running and stopped on paused/done (freeze-in-place per D-02 Decorative Loop Contract), a per-frame callback that reads context.read<TimerController>().progress fresh and setState only when it changed, a `double get progress`, and a `double loopPhase(Duration period)` helper deriving decorative-loop phase from the ticker's elapsed time (never a second AnimationController). Watch TimerController.phase via context.watch in didChangeDependencies solely to start/stop the ticker; never pass phase into painting. Mirror the abstraction-boundary discipline of ScenePreviewPainter (per D-06 carried forward).

    Create disc_painter.dart with a top-level pure function `Color discColorForRemaining(double remaining)` exactly per RESEARCH Pattern 3 (green Color(0xFF7FA87A); yellow→green lerp Color(0xFFE8B75A)→green with t=(remaining-0.2)/0.3 for 0.2<remaining≤0.5; red→yellow lerp Color(0xFFDE6A4B)→yellow with t=remaining/0.2 for remaining≤0.5... i.e. remaining≤0.2), and `DiscPainter extends CustomPainter` taking `progress` (and, if used, loop phase) as constructor fields. In paint: fill background const Color(0xFFF6EBDD) (AppTokens.bg); draw the centered dashed track ring 310×310 logical px, 2px stroke, color const Color(0x244B4038) (rgba(75,64,56,0.14)), default on per §C; draw the disc as a filled circle centered on screen with drawn radius = 150 * max(0.001, remaining) where remaining = (1 - progress).clamp(0.0,1.0) — replicate the 0.001 floor exactly so the disc never reaches a degenerate zero radius; fill color = discColorForRemaining(remaining); draw the constant disc shadow blur (offset (0,20), blur ~55, color const Color(0x1A000000) = rgba(0,0,0,0.10)) that does not scale with disc size. Cache progress-independent Paint objects as fields per Pitfall 5. Implement shouldRepaint to compare both progress and loop-phase fields (Pitfall 2). Do not reproduce any CSS transition/ease timing — draw the progress-derived value directly (§Scene Geometry common conventions).

    Create disc_scene.dart: `DiscScene extends SceneRenderer`; its state extends `SceneRendererState<DiscScene>` and returns a full-bleed CustomPaint(painter: DiscPainter(progress: progress, ...)) in build. No Scaffold, no AppBar, no GestureDetector, no Text anywhere in the scene (SCENE-05).

    Create test/support/progress_sweep.dart: a shared helper that pumps a scene widget across fixed progress checkpoints (e.g. 0.0, 0.25, 0.5, 0.75, 1.0) using tester.pump(fixedDuration) — never pumpAndSettle (Pitfall 4, infinite decorative loops). It should drive an injected-clock TimerController so progress advances deterministically across the sweep. Write all three test files to the behaviors above.
  </action>
  <verify>
    <automated>flutter test test/scenes/disc/ test/scenes/scene_renderer_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/scenes/disc/disc_painter_test.dart` passes with the discColorForRemaining threshold assertions in <behavior>.
    - `flutter test test/scenes/disc/disc_scene_test.dart` passes: DiscScene renders without throwing across the full 0.0→1.0 sweep and its subtree has zero Text and zero gesture-reactive ancestors.
    - `flutter test test/scenes/scene_renderer_test.dart` passes: a test SceneRendererState subclass starts its ticker on running and stops on paused/done, and polls progress via context.read.
    - `flutter analyze` reports no new errors in lib/scenes/scene_renderer.dart or lib/scenes/disc/.
  </acceptance_criteria>
  <done>DiscScene renders the correct color-zone/size disc for any progress in 0..1, driven by a per-scene ticker; discColorForRemaining is a unit-tested pure function; no scene-level gesture handler or text exists.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Disc into the running app (registry + RunningScreen + Setup navigation)</name>
  <files>lib/scenes/scene_registry.dart, lib/screens/running_screen.dart, lib/screens/setup_screen.dart, test/scenes/scene_registry_test.dart</files>
  <read_first>
    - lib/screens/placeholder_running_screen.dart (auto-pop-on-done, single-pop guard, back IconButton, Semantics — all preserved in RunningScreen)
    - lib/screens/setup_screen.dart (_handleStart lines ~114-126; _theme state; existing Navigator.push to PlaceholderRunningScreen)
    - lib/widgets/scene_grid.dart (the SceneTheme→painter map "one switch" precedent, D-06)
    - lib/scenes/scene_theme.dart (SceneTheme enum: disc, sunrise, walk, car)
    - lib/theme/app_tokens.dart (AppTokens.bg, AppTokens.ink for the RunningScreen chrome)
    - .planning/phases/03-scene-themes/03-PATTERNS.md (running_screen.dart and scene_registry.dart pattern assignments)
    - .planning/phases/03-scene-themes/03-UI-SPEC.md (Interaction Contract; per-scene base background colors for the pending fallback)
    - test/screens/setup_screen_test.dart (existing harness + Start-navigation expectations that must keep passing)
  </read_first>
  <action>
    Create scene_registry.dart exposing `Widget sceneFor(SceneTheme theme)` — the single place allowed to name concrete scene widgets (mirrors SceneGrid._painters per D-06). In this plan the switch returns `DiscScene()` for SceneTheme.disc and, for sunrise/walk/car, an interim private `_PendingScene(theme)` (a StatelessWidget rendering a full-bleed ColoredBox of that theme's locked base background color per §D/§E/§F: sunrise Color(0xFF182449), walk Color(0xFFBFE0EE), car Color(0xFFF6D9A8)) — a calm, non-crashing placeholder replaced by real scenes in Plans 02–03. Add a code comment marking the pending arms as replaced in 03-02/03-03. The pending fallback is interim scaffolding within this phase, not a shipped scope reduction — Plans 02 and 03 deliver the real Sunrise/Walk/Car scenes.

    Create running_screen.dart as `RunningScreen extends StatefulWidget` with a required `final SceneTheme theme`. Port from placeholder_running_screen.dart verbatim: the `_leftScreen` single-pop guard, `_leaveOnce()`, `_handleBack()` (endTimer + leave), and `_maybeAutoPopWhenDone(phase)` post-frame auto-pop; keep `context.watch<TimerController>()` only for phase (auto-pop trigger). Replace the inline Transform.scale accent circle body with `sceneFor(widget.theme)` inside the Stack. Keep the Scaffold backgroundColor AppTokens.bg and the top-left back IconButton with its Semantics wrapper (composition-root affordance — Phase 4 replaces it with the hidden long-press; it must stay on RunningScreen, never inside a scene, per SCENE-05 + PATTERNS.md).

    Modify setup_screen.dart _handleStart: change the Navigator.push target from `const PlaceholderRunningScreen()` to `RunningScreen(theme: _theme)`, threading the selected scene theme through. Leave the TimerController.start(_selectedMinutes) call and the fire-and-forget SetupPreferences.persistIfPreset(...) exactly as-is. Update the import to running_screen.dart.

    Write scene_registry_test.dart asserting sceneFor(SceneTheme.disc) is a DiscScene, and sceneFor returns a non-null Widget for sunrise/walk/car (pending fallback) in this plan.
  </action>
  <verify>
    <automated>flutter test test/scenes/scene_registry_test.dart test/screens/setup_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/scenes/scene_registry_test.dart` passes: sceneFor(disc) returns a DiscScene; sceneFor(sunrise/walk/car) returns a non-null Widget.
    - `flutter test test/screens/setup_screen_test.dart` still passes (Start navigation continues to work with the new RunningScreen target).
    - `flutter test` (full suite) is green.
    - `flutter analyze` reports no new errors; setup_screen.dart imports running_screen.dart and no longer constructs PlaceholderRunningScreen in _handleStart.
  </acceptance_criteria>
  <done>Pressing Start with the Disc theme selected navigates to RunningScreen, which hosts the animated DiscScene; the other three themes render a calm pending background pending Plans 02–03; back-nav and auto-pop-on-done behave exactly as the placeholder did.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TimerController.progress → scene painter | Numeric progress crosses into per-frame draw math; the only "input" this phase consumes. Already clamped 0..1 upstream. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Denial of Service | DiscPainter draw math (radius/scale) | low | mitigate | Re-clamp remaining = (1-progress).clamp(0,1) and apply the 0.001 radius floor so no degenerate/negative draw call crashes the render loop. |
| T-03-02 | Tampering | scene_registry sceneFor switch | low | accept | Registry maps a fixed enum to widgets; no external/untrusted input reaches it. No mitigation beyond the exhaustive-switch discipline. |

No high-severity threats (block-on threshold = high). This phase adds zero network, auth, persistence, or new packages (RESEARCH Security Domain).
</threat_model>

<verification>
- `flutter test test/scenes/ test/screens/setup_screen_test.dart` green.
- `flutter test` full suite green.
- `flutter analyze` no new errors.
- End-of-phase human check (D-03, deferred to phase gate): on a low/mid-end Android device (API 24–28) or a throttled emulator as lower-confidence fallback, the Disc scene shrinks smoothly with no visible stepping/jank (SCENE-05 perceptual criterion — recorded as a UAT item, not an automated gate).
</verification>

<success_criteria>
- A parent can select Shrinking Disc, press Start, and watch a full-screen disc shrink and shift green→yellow→red, arriving at a tiny pure-red disc at time-up (SCENE-01).
- The disc scene has no text and nothing tappable within the scene subtree (SCENE-05).
- Motion is ticker-driven (per-frame), not capped at the 200ms notify cadence.
</success_criteria>

## Artifacts this phase produces (Plan 01)

New symbols/files created by this plan:
- `lib/scenes/scene_renderer.dart`: `SceneRenderer` (abstract StatefulWidget), `SceneRendererState<T>` (mixin state with `progress` getter and `loopPhase(Duration)` method).
- `lib/scenes/scene_registry.dart`: top-level `Widget sceneFor(SceneTheme theme)`; private `_PendingScene` (interim fallback).
- `lib/scenes/disc/disc_painter.dart`: top-level `Color discColorForRemaining(double remaining)`; `DiscPainter extends CustomPainter`.
- `lib/scenes/disc/disc_scene.dart`: `DiscScene extends SceneRenderer`.
- `lib/screens/running_screen.dart`: `RunningScreen extends StatefulWidget` with a `SceneTheme theme` field (replaces PlaceholderRunningScreen as Start's destination).
- `test/support/progress_sweep.dart`: shared progress-sweep pump helper.
- Modified: `lib/screens/setup_screen.dart` (_handleStart now navigates to `RunningScreen(theme: _theme)`).

<output>
Create `.planning/phases/03-scene-themes/03-01-SUMMARY.md` when done.
</output>

---
phase: 02-setup-screen
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - lib/widgets/hold_repeat_button.dart
  - lib/screens/setup_screen.dart
  - test/widgets/hold_repeat_button_test.dart
  - test/screens/setup_screen_test.dart
autonomous: true
requirements: [SETUP-02]
must_haves:
  truths:
    - "Tapping 'Custom' reveals a stepper row with -, a value block, and + (grid does not reflow)."
    - "Tapping - or + changes customMin by exactly 1; holding either repeats and accelerates (~500ms -> 350ms -> 150ms -> 60ms)."
    - "The - button disables at customMin==1 and + disables at customMin==120; the repeat Timer never survives release, preset-switch, or widget disposal."
    - "customMin can never leave 1..120 even if disable logic has a bug — the state setter itself clamps (V5)."
  artifacts:
    - lib/widgets/hold_repeat_button.dart
    - test/widgets/hold_repeat_button_test.dart
  key_links:
    - "HoldRepeatButton(onStep, enabled, child): parent owns clamp/disable via enabled + a clamped onStep; the button owns Timer cancel-on-every-exit-path + dispose"
    - "SetupScreen clamps customMin in its setter (not only via disabled buttons); Start uses showCustom ? customMin : durationMin"
---

<objective>
Add a custom duration path: a "Custom" option that reveals an accelerating hold-repeat +/- stepper covering the full 1-120 minute range without requiring up to 119 taps, with clear disabled edges and a range clamp enforced in state (not just visually).

Purpose: Satisfy SETUP-02 and the V5 Input Validation control (clamp customMin in the setter), while establishing a reusable, leak-safe `HoldRepeatButton`.
Output: `HoldRepeatButton` widget + its test, and the Custom stepper row integrated into `SetupScreen`.
</objective>

## Phase Goal (MVP user story)

**As a** parent, **I want to** configure a countdown's duration and scene and launch it with my last-used settings pre-selected, **so that** my child can watch a wordless visual timer without me re-entering settings each time.

This plan adds the custom-duration slice. After it, a parent can dial any 1-120 minute value, not just presets.

## Artifacts this phase produces (this plan's contribution)

- `HoldRepeatButton` (`lib/widgets/hold_repeat_button.dart`) — tap = one step, hold = accelerating repeat; leak-safe `Timer` lifecycle.
- Custom stepper region in `SetupScreen`: `showCustom` state, `customMin` (default 3), the two `HoldRepeatButton`s, value block, and disabled-edge affordances.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-setup-screen/02-UI-SPEC.md
@.planning/phases/02-setup-screen/02-RESEARCH.md
@.planning/phases/02-setup-screen/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HoldRepeatButton (accelerating hold-repeat, leak-safe) + tests (SETUP-02)</name>
  <files>lib/widgets/hold_repeat_button.dart, test/widgets/hold_repeat_button_test.dart</files>
  <read_first>
    - lib/timer/timer_controller.dart (Timer lifecycle discipline: cancel before recreate, cancel in dispose — lines around start()/dispose())
    - .planning/phases/02-setup-screen/02-RESEARCH.md ("Pattern 1: Hold-Repeat Stepper", Pitfalls 1 & 2)
    - .planning/phases/02-setup-screen/02-UI-SPEC.md (Interaction Contract: hold curve ~500ms->350ms->150ms->60ms, disabled-edge behavior)
    - test/timer/timer_controller_test.dart (group/test naming, explicit teardown/dispose style)
  </read_first>
  <behavior>
    - Single tap (release before ~500ms) fires onStep exactly once, no repeat.
    - Held past ~500ms begins repeating and accelerates; when enabled flips false the repeat stops.
    - The repeat Timer is cancelled on long-press end, on long-press cancel, AND on dispose (no setState-after-dispose, no ghost steps after the widget unmounts mid-hold).
  </behavior>
  <action>
    Create lib/widgets/hold_repeat_button.dart as a `StatefulWidget` with required `VoidCallback onStep`, `bool enabled`, and `Widget child`, using a `GestureDetector`: `onTap` (when enabled) fires one `onStep`; `onLongPressStart` (when enabled) begins a self-rescheduling single-shot `Timer` whose interval accelerates by hold duration — ~350ms after the ~500ms long-press threshold, ~150ms after 2s, ~60ms after 4s (recompute the interval each tick so it accelerates mid-hold); `onLongPressEnd` and `onLongPressCancel` stop repeating. Cancel the Timer in `dispose()` AND in the end/cancel handlers (Pitfalls 1 & 2 — never rely on one path). If `enabled` becomes false mid-repeat, stop. Use `Timer` (single-shot, rescheduled), not `Timer.periodic`. Add `///` docs explaining the dispose-cancel rationale. The widget must know nothing about the 1-120 range — the parent supplies `enabled` and a clamped `onStep`.
    Create test/widgets/hold_repeat_button_test.dart covering: (a) a quick tap fires onStep once; (b) a long-press held across the accelerating thresholds fires onStep multiple times at increasing frequency (use `tester.pump(Duration)` to advance); (c) with `enabled: false`, neither tap nor hold fires onStep; (d) Pitfall 1/2 — starting a hold then unmounting the widget (pump a widget tree that removes it) throws no exception and produces no further onStep calls; (e) releasing (long-press end) stops further steps.
  </action>
  <verify>
    <automated>cd D:/Projects/zual && flutter test test/widgets/hold_repeat_button_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Tap-once, hold-accelerate, disabled-no-op, and dispose-mid-hold (no exception, no ghost steps) are all covered and green.
    - The Timer is cancelled on end, cancel, and dispose; implementation uses a rescheduling single-shot Timer.
    - HoldRepeatButton contains no 1-120 range logic (range ownership stays with the parent).
  </acceptance_criteria>
  <done>A reusable, leak-safe accelerating hold-repeat button exists and is proven by tests including the dispose-mid-hold case.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate Custom stepper into SetupScreen with clamped setter + disabled edges (SETUP-02, V5)</name>
  <files>lib/screens/setup_screen.dart, test/screens/setup_screen_test.dart</files>
  <read_first>
    - lib/screens/setup_screen.dart (existing duration grid, preset selection state, Start label logic)
    - .planning/phases/02-setup-screen/02-UI-SPEC.md (Custom button copy "Custom"/"set your own", stepper glyphs/value typography, Spacing "Custom stepper row", disabled-edge ~35% opacity, customMin default 3)
    - lib/widgets/hold_repeat_button.dart (the button API to compose)
    - lib/timer/timer_controller.dart (clamp-on-write precedent: minutes.clamp(_minMinutes, _maxMinutes))
  </read_first>
  <behavior>
    - Selecting a preset while Custom is open hides the stepper row; selecting Custom reveals it.
    - customMin starts at 3 the first time Custom is shown in a session and is not reset by toggling the row closed/open.
    - customMin stays within 1..120 for any sequence of steps; - disabled at 1, + disabled at 120.
    - Start uses customMin when showCustom is true, else durationMin.
  </behavior>
  <action>
    In lib/screens/setup_screen.dart add a sixth duration-grid cell "Custom" ("Custom" 19/700 + "set your own" 11/600 sublabel per UI-SPEC) that sets `showCustom = true`; tapping any numeric preset sets `showCustom = false`. Add `int _customMin` (default 3) and a private setter `_setCustomMin(int v)` that assigns `v.clamp(1, 120)` — this clamp is the V5 control and must be independent of button-disable state (a bug in the disable logic must not be able to push customMin out of range). When `showCustom`, render below the grid (grid does not reflow) a stepper row: a `HoldRepeatButton` "-" (48px circular #F1E6D3, glyph 26/700) with `enabled: _customMin > 1` and `onStep: () => setState(() => _setCustomMin(_customMin - 1))`; a center value block (value 36/700 + "minutes" 12/600); a `HoldRepeatButton` "+" with `enabled: _customMin < 120` and `onStep: () => setState(() => _setCustomMin(_customMin + 1))`. Disabled buttons render at ~35% opacity on fill and glyph and do not respond to tap/hold (pass `enabled: false`), and are excluded from tap semantics while disabled. The Start suffix and `TimerController.start` argument use `showCustom ? _customMin : _durationMin`. The selection ring moves to the Custom card when showCustom is true.
    Extend test/screens/setup_screen_test.dart: tapping Custom reveals the stepper and moves selection; tapping + increments the value and Start then calls start() with the custom value; holding - down to 1 disables - (assert the disabled state) and further steps do not go below 1; selecting a preset while Custom is open hides the stepper. Add a direct-state assertion that stepping past the edges cannot produce a value outside 1..120 (V5).
    Do NOT modify lib/timer/.
  </action>
  <verify>
    <automated>cd D:/Projects/zual && flutter test test/screens/setup_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Custom reveals an accelerating +/- stepper; - disables at 1, + disables at 120 (visually ~35% and non-interactive).
    - customMin is clamped to 1..120 in `_setCustomMin` regardless of disable logic; tests assert no out-of-range value is reachable (V5, threat T-02-01).
    - Start uses the custom value when showCustom; selecting a preset hides the stepper; tests green; lib/timer/ unmodified.
  </acceptance_criteria>
  <done>SETUP-02 is met: a parent can set any 1-120 minute value via an accelerating stepper with hard-clamped state and clear disabled edges.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stepper interaction -> customMin state -> TimerController.start | Local UI input; the only value-integrity concern is keeping customMin inside 1..120 through any gesture/edge sequence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | SetupScreen `_setCustomMin` / customMin (V5 Input Validation) | high | mitigate | Clamp customMin to 1..120 inside the state setter itself, independent of button-disable logic, so no code path (including a disable-logic bug or accelerated repeat overshoot) can push the value out of range. Verified by a direct-state test asserting bounds after edge stepping. |
| T-02-05P | Denial of Service | HoldRepeatButton repeat Timer | medium | mitigate | Cancel the Timer on long-press end, long-press cancel, and dispose; use a rescheduling single-shot Timer. Verified by the dispose-mid-hold test (no setState-after-dispose, no ghost steps). |
</threat_model>

<verification>
- `flutter test test/widgets/hold_repeat_button_test.dart` and `flutter test test/screens/setup_screen_test.dart` green.
- `flutter analyze` clean for changed files; lib/timer/ untouched.
</verification>

<success_criteria>
- SETUP-02: accelerating hold-repeat stepper across 1-120 with disabled edges.
- V5/T-02-01: customMin provably cannot leave 1..120 (clamp in setter, asserted in tests).
- No Timer leak: dispose-mid-hold is exception-free with no further steps.
</success_criteria>

<output>
Create `.planning/phases/02-setup-screen/02-03-SUMMARY.md` when done.
</output>

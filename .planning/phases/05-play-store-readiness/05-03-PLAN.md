---
phase: 05-play-store-readiness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/index.html
  - .planning/phases/05-play-store-readiness/05-STORE-LISTING.md
autonomous: false
requirements: [PUBLISH-02]
user_setup:
  - service: github-pages
    why: "Play Console requires a publicly-reachable privacy policy URL (D-09/D-10). GitHub Pages must be enabled in repo settings and the content pushed to origin — a repo-settings toggle plus a push the automated agent should not perform unprompted."
    dashboard_config:
      - task: "Enable GitHub Pages from branch main, folder /docs; confirm the published URL returns 200"
        location: "github.com/reiteristvan/zual -> Settings -> Pages"

must_haves:
  truths:
    - "A privacy policy page states plainly that Zual has no accounts, collects no data, shows no ads, and works fully offline."
    - "A store-listing answer sheet captures the exact display name, content-rating answers, target-audience declaration, and store description the human will paste into Play Console."
    - "The content-rating/target-audience answers are internally consistent with the locked decisions (Everyone tier; general audience that also appeals to children; not Designed for Families)."
    - "The privacy policy is reachable at a stable public GitHub Pages URL."
  artifacts:
    - "docs/index.html — static privacy policy page"
    - ".planning/phases/05-play-store-readiness/05-STORE-LISTING.md — Play Console answer sheet + descriptions"
  key_links:
    - "docs/index.html -> GitHub Pages (main /docs) -> https://reiteristvan.github.io/zual/ -> Play Console 'Target audience and content' privacy-policy field"
    - "05-STORE-LISTING.md answers -> human -> Play Console content-rating (IARC) + target-audience questionnaire"
---

<objective>
Prepare every Play Console listing artifact this phase can prepare without a device: a drafted
static privacy policy page (D-09) to be hosted on GitHub Pages (D-10), and a single
answer-sheet document capturing the display name (D-06), the target-audience declaration
(D-07), the content-rating answers (D-08), and the short/full store description in D-14's
parent-practical tone. These are prepared-but-not-submitted artifacts — the human pastes them
into Play Console and re-verifies wording against the live questionnaire at submission time
(STATE.md blocker; RESEARCH Pitfall 5).

Purpose: PUBLISH-02 — a listing needs a live privacy-policy URL and a correct, decision-aligned
answer set before it can be submitted.
Output: `docs/index.html`, `.planning/phases/05-play-store-readiness/05-STORE-LISTING.md`, and
a live GitHub Pages URL.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-play-store-readiness/05-CONTEXT.md
@.planning/phases/05-play-store-readiness/05-RESEARCH.md
</context>

<mvp_slice>
Delivers the listing-readiness slice: after this plan, a human has a live privacy-policy URL
and a copy-paste-ready answer sheet — everything Play Console's "Target audience and content"
and store-listing text fields require, short of the on-device screenshots (plan 05-05). Each
task produces a real, reviewable artifact, not scaffolding.
</mvp_slice>

<tasks>

<task type="auto">
  <name>Task 1: Draft the static privacy policy page</name>
  <files>docs/index.html</files>
  <read_first>
    - .planning/phases/05-play-store-readiness/05-CONTEXT.md (D-09 privacy policy required though nothing is collected; D-10 host on GitHub Pages)
    - .planning/PROJECT.md (Core Value and constraints — app is fully local, no accounts, no network)
    - .planning/REQUIREMENTS.md (Out of Scope: no accounts, no network/backend, only local storage of last-used settings)
  </read_first>
  <action>
    Create docs/index.html as a single self-contained static HTML page (no external CSS/JS/CDN
    — it must render correctly on GitHub Pages with zero build step). Title it "Zual — Privacy
    Policy". State plainly and truthfully: Zual has no user accounts and no login; it collects,
    stores, and transmits NO personal data; it contains no advertising and no third-party
    analytics or tracking SDKs; it works fully offline; the only data it stores is the last-used
    duration and theme, kept locally on the device via the OS's standard app storage and never
    sent anywhere. Include a plain-language "Children's privacy" sentence noting the app is
    parent-operated and collects nothing from anyone including children (supports the D-07
    general-audience-that-also-appeals-to-children declaration and Families Policy privacy
    requirement). Include a "Contact" line with the developer email reiteristvan@gmail.com and
    an effective date of 2026-07-09. Keep it short, readable, and inline-styled for legibility.
    Do NOT claim any data collection, permissions, or third-party services that do not exist.
  </action>
  <verify>
    <automated>cd "D:/Projects/zual" && test -f docs/index.html && grep -qi "privacy" docs/index.html && grep -qi "no.*data\|collects no\|does not collect" docs/index.html && grep -q "reiteristvan@gmail.com" docs/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `docs/index.html` exists and is valid standalone HTML (opens with a doctype/`<html>`, no external resource references).
    - Page text asserts no accounts, no data collection, no ads, offline operation, and local-only last-used settings.
    - Page contains a children's-privacy statement and the contact email `reiteristvan@gmail.com`.
    - No claim of data collection, tracking, or third-party SDKs appears (truthful to the actual app).
  </acceptance_criteria>
  <done>A truthful, self-contained privacy policy page exists at docs/index.html, ready for GitHub Pages hosting.</done>
</task>

<task type="auto">
  <name>Task 2: Draft the Play Console answer sheet and store descriptions</name>
  <files>.planning/phases/05-play-store-readiness/05-STORE-LISTING.md</files>
  <read_first>
    - .planning/phases/05-play-store-readiness/05-CONTEXT.md (D-06 display name; D-07 target audience; D-08 content rating lowest tier; D-14 description tone)
    - .planning/phases/05-play-store-readiness/05-RESEARCH.md (Pitfall 5: answers are a draft to re-verify at submission; Families Policy still requires the privacy policy)
    - .planning/STATE.md (Blockers/Concerns: re-verify Families Policy/target-audience wording in Play Console at submission time)
  </read_first>
  <action>
    Create .planning/phases/05-play-store-readiness/05-STORE-LISTING.md as the human's
    copy-paste answer sheet. Include: (1) App display name exactly "Zual — Visual Timer for
    Kids" (D-06). (2) Target-audience declaration: general audience that also appeals to
    children, NOT the "Designed for Families" track (D-07) — with the one-line rationale
    (parent-operated; zero ads/accounts/data collection). (3) Content-rating (IARC)
    questionnaire answers aiming for the lowest/Everyone tier (D-08): no violence, no sexual
    content, no profanity/text, no user-generated content, no ads, no in-app purchases, no
    location/data sharing, no interactive elements beyond a child watching — enumerate each
    common IARC question with the intended answer. (4) Privacy policy URL:
    `https://reiteristvan.github.io/zual/` (from Task 1 / Task 3). (5) A short store description
    (<= ~80 chars, D-14 parent-practical, leads with the problem solved — a wordless countdown
    a young child can read from across the room) and a full description (a few short paragraphs,
    same tone, mirroring PROJECT.md's Core Value; no numbers/clock framing). Add a prominent
    "RE-VERIFY AT SUBMISSION" note (Pitfall 5 / STATE.md blocker): the human must re-check these
    answers against the live Play Console questionnaire wording, since policy text drifts. Mark
    this file explicitly as a prepared draft, not a submitted declaration.
  </action>
  <verify>
    <automated>cd "D:/Projects/zual" && test -f .planning/phases/05-play-store-readiness/05-STORE-LISTING.md && grep -q "Zual — Visual Timer for Kids" .planning/phases/05-play-store-readiness/05-STORE-LISTING.md && grep -qi "designed for families" .planning/phases/05-play-store-readiness/05-STORE-LISTING.md && grep -q "https://reiteristvan.github.io/zual/" .planning/phases/05-play-store-readiness/05-STORE-LISTING.md && grep -qi "re-verify" .planning/phases/05-play-store-readiness/05-STORE-LISTING.md</automated>
  </verify>
  <acceptance_criteria>
    - `05-STORE-LISTING.md` exists and contains the exact display name `Zual — Visual Timer for Kids` (D-06).
    - It states the target audience is general-audience-that-also-appeals-to-children and explicitly NOT "Designed for Families" (D-07).
    - It enumerates IARC content-rating answers all pointing to the Everyone/lowest tier (D-08).
    - It contains the privacy policy URL `https://reiteristvan.github.io/zual/`.
    - It contains a short description and a full description in D-14's parent-practical tone.
    - It contains a "RE-VERIFY AT SUBMISSION" note referencing the live Play Console questionnaire.
  </acceptance_criteria>
  <done>A single answer-sheet doc holds the display name, target-audience declaration, content-rating answers, privacy URL, and both store descriptions — copy-paste-ready and flagged for submission-time re-verification.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Enable GitHub Pages and confirm the privacy policy URL is live</name>
  <action>Pause execution and wait for the developer to push the phase commits, enable GitHub
  Pages (branch main, folder /docs), and confirm the published URL returns 200 with the correct
  content, per the steps below. Do not toggle repo settings or force-push automatically. Resume
  only after "approved".</action>
  <what-built>docs/index.html is a complete static privacy policy page and
  05-STORE-LISTING.md records the URL it must be served from. GitHub Pages must be enabled and
  the content pushed to origin for the URL to go live — a repo-settings toggle plus a push the
  agent should not perform unprompted.</what-built>
  <how-to-verify>
    1. Ensure this phase's commits (including docs/index.html) are pushed to `origin/main`.
    2. In the browser: github.com/reiteristvan/zual -> Settings -> Pages -> Source: "Deploy
       from a branch" -> Branch: `main`, Folder: `/docs` -> Save.
    3. Wait for the first Pages deploy to finish (usually ~1 min), then confirm the URL is live:
       `curl -sI https://reiteristvan.github.io/zual/` should return `HTTP/… 200`.
    4. Open https://reiteristvan.github.io/zual/ in a browser and confirm the privacy policy
       text renders and matches docs/index.html (no accounts, no data collection, no ads,
       offline, contact email present).
    5. Type "approved" once the URL returns 200 and the content is correct.
  </how-to-verify>
  <resume-signal>Type "approved" (privacy policy live at https://reiteristvan.github.io/zual/ returning 200) or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> public GitHub Pages | docs/ content becomes world-readable; must contain no secrets |
| privacy policy URL -> Play Console listing | the page's availability backs a required listing field |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Information Disclosure | docs/index.html on public Pages | low | mitigate | Page is intentionally public and contains only the privacy statement + a contact email the developer already publishes; no secret, key, or internal path is included. Verified by content review at the checkpoint. |
| T-05-06 | Denial of (listing) availability | GitHub Pages hosting the privacy URL | low | accept | A plain static page on GitHub Pages is the industry-standard low-risk host for this use case (RESEARCH Security Domain); outage would only affect the Play Console link, not the app. Accepted, no over-engineering. |
</threat_model>

<verification>
- `docs/index.html` and `05-STORE-LISTING.md` exist with the asserted content (Tasks 1-2).
- `curl -sI https://reiteristvan.github.io/zual/` returns 200 after the checkpoint (Task 3).
- `flutter test` (full existing suite) stays green — this plan adds no app code.
</verification>

<success_criteria>
- A truthful privacy policy is live at a stable public URL (PUBLISH-02 privacy requirement).
- A decision-aligned, copy-paste-ready Play Console answer sheet exists (PUBLISH-02
  content-rating/target-audience declaration prepared for human submission).
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 05-03)

- `docs/index.html` — NEW: self-contained static privacy policy page (served via GitHub Pages).
- `.planning/phases/05-play-store-readiness/05-STORE-LISTING.md` — NEW: Play Console answer sheet (display name D-06, target audience D-07, IARC content-rating answers D-08, privacy URL, short + full store descriptions D-14, submission-time re-verify note).
- GitHub Pages site at `https://reiteristvan.github.io/zual/` — NEW public URL (developer-enabled).
</artifacts>

<output>
Create `.planning/phases/05-play-store-readiness/05-03-SUMMARY.md` when done.
</output>
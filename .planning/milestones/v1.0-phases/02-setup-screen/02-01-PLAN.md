---
phase: 02-setup-screen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/theme/app_tokens.dart
  - lib/screens/setup_screen.dart
  - lib/screens/placeholder_running_screen.dart
  - lib/main.dart
  - test/screens/setup_screen_test.dart
  - test/widget_test.dart
autonomous: true
requirements: [SETUP-01, SETUP-04]
must_haves:
  truths:
    - "Launching the app shows the Setup screen, not the Hello World scaffold."
    - "Tapping a preset (1/5/10/15/30 min) selects it and draws a 3px #7FA87A ring overlay."
    - "Tapping Start calls TimerController.start(selectedMinutes) and navigates to the placeholder running screen (SETUP-04)."
    - "The placeholder running screen shows a shrinking #7FA87A circle scaled by (1 - TimerController.progress), returns to Setup on the back control via endTimer(), and auto-returns when TimerPhase.done is reached."
  artifacts:
    - lib/theme/app_tokens.dart
    - lib/screens/setup_screen.dart
    - lib/screens/placeholder_running_screen.dart
    - test/screens/setup_screen_test.dart
  key_links:
    - "Start button onPressed -> TimerController.start(minutes) + Navigator push of PlaceholderRunningScreen"
    - "PlaceholderRunningScreen consumes TimerController via context.watch; phase==done -> pop, back control -> endTimer()+pop"
---

<objective>
Deliver the phase's walking slice: a parent launches the app, picks a preset duration, taps Start, watches a bare placeholder countdown, and returns to Setup. This is the thinnest real end-to-end path through the Setup feature and replaces the Hello World scaffold as the app home.

Purpose: Establish the `lib/screens/` + `lib/theme/` structure and prove the Setup -> running -> Setup loop works against the existing (Phase 1) `TimerController` before layering scene selection, custom durations, persistence, and pixel-accurate polish in later plans.
Output: `AppTokens` design-token class, a working `SetupScreen` (duration presets + Start), a `PlaceholderRunningScreen`, rewired `main.dart`, and the E2E widget test that pins SETUP-01/SETUP-04.
</objective>

## Phase Goal (MVP user story)

**As a** parent, **I want to** configure a countdown's duration and scene and launch it with my last-used settings pre-selected, **so that** my child can watch a wordless visual timer without me re-entering settings each time.

This plan delivers the duration + launch spine of that story; scene selection (Plan 02), custom durations (Plan 03), persistence (Plan 04), and Layout A fidelity (Plan 05) complete it.

## Artifacts this phase produces (this plan's contribution)

- `AppTokens` (`lib/theme/app_tokens.dart`) — static color/spacing/radius/text-style constants sourced from 02-UI-SPEC.md and `design/README.md` (system font for now; Plan 05 swaps in bundled Baloo 2 / Quicksand).
- `SetupScreen` (`lib/screens/setup_screen.dart`) — the parent-facing home screen (duration presets + Start in this plan).
- `PlaceholderRunningScreen` (`lib/screens/placeholder_running_screen.dart`) — inert Start destination (D-01..D-04 stand-in).
- Updated `main.dart`: async prefs-ready `MyApp`, `SetupScreen` as `home`, `MyHomePage` deleted.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-setup-screen/02-UI-SPEC.md
@.planning/phases/02-setup-screen/02-CONTEXT.md
@.planning/phases/02-setup-screen/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Failing end-to-end widget test for duration select + Start (RED)</name>
  <files>test/screens/setup_screen_test.dart</files>
  <read_first>
    - test/widget_test.dart (existing widget-test scaffold shape)
    - test/timer/timer_controller_test.dart (group/test naming, explicit dispose, injected-clock TimerController)
    - .planning/phases/02-setup-screen/02-VALIDATION.md (Per-Task Verification Map — SETUP-01, SETUP-04)
    - lib/timer/timer_controller.dart (start/endTimer/phase/progress public API this screen drives)
  </read_first>
  <action>
    Create test/screens/setup_screen_test.dart as the failing E2E contract for this slice. Import `package:zual/screens/setup_screen.dart` and `package:zual/screens/placeholder_running_screen.dart` (which Tasks 2-3 create). Construct a `SetupScreen` inside a `MaterialApp` + `ChangeNotifierProvider<TimerController>.value` using an injected-clock `TimerController(clock: () => fixedTime)` so no wall-clock flakiness. Use `group('SetupScreen', ...)` matching the existing test style.
    Write three tests: (a) SETUP-01 — the five preset labels 1/5/10/15/30 render and tapping the "10" preset selects it (assert a selection indicator widget/finder becomes present on that card); (b) SETUP-04 — after selecting "10" and tapping the Start button (find by its "Start" text run), `TimerController.start` was called with 10 (assert via the controller's resulting phase becoming `TimerPhase.running` and a `PlaceholderRunningScreen` being present in the tree after `pumpAndSettle`); (c) default selection — on first pump with no prior selection, the "5" preset is pre-selected (D-09 default duration).
    This test is expected to be RED until Tasks 2-3 land — it will not compile/pass yet because SetupScreen and PlaceholderRunningScreen do not exist. That is the intended failing state.
    Do NOT reference any custom-stepper, scene-card, or persistence symbols here — those belong to Plans 02-04's own test additions.
  </action>
  <verify>
    <automated>cd D:/Projects/zual && flutter test test/screens/setup_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - test/screens/setup_screen_test.dart exists and encodes assertions for SETUP-01 (preset select), SETUP-04 (Start -> start(minutes) + navigation), and the 5-min default.
    - Running the file is RED (fails to compile or fails assertions) because SetupScreen/PlaceholderRunningScreen are not yet implemented — the expected pre-implementation state that Tasks 2-3 turn GREEN.
    - Uses an injected-clock TimerController (no real DateTime.now dependency).
  </acceptance_criteria>
  <done>The E2E test file pins the duration-select and Start contract and is RED pending Tasks 2-3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AppTokens + SetupScreen (duration presets + Start) + main.dart rewire</name>
  <files>lib/theme/app_tokens.dart, lib/screens/setup_screen.dart, lib/main.dart, test/widget_test.dart</files>
  <read_first>
    - .planning/phases/02-setup-screen/02-UI-SPEC.md (Spacing Scale, Typography, Color, Copywriting, Interaction Contract)
    - design/README.md sections "A. Setup — Layout A (default)" and "Design Tokens" (authoritative radii/shadows: buttons 22px, cards 26px, Start radius 26px + shadow 0 8px 20px rgba(127,168,122,0.4), card shadow 0 2px 6px rgba(75,64,56,0.05))
    - lib/main.dart (current app root: WidgetsFlutterBinding, ChangeNotifierProvider.value, MyApp shape to extend)
    - lib/timer/timer_controller.dart (start(int minutes) — the only TimerController call Start makes)
  </read_first>
  <behavior>
    - Tapping preset "10" then Start results in TimerController.phase == running (start called with 10).
    - The "5" preset is selected by default before any tap.
    - The Start label shows the selected minute count ("Start" + "· 5 min", updating to "· 10 min" after selecting 10).
  </behavior>
  <action>
    Create lib/theme/app_tokens.dart exposing an `AppTokens` class of static const values transcribed from 02-UI-SPEC.md and design/README.md: colors `bg` #F6EBDD, `cardSurface` #FFFCF6, `stepperFill` #F1E6D3, `accent` #7FA87A, `accentPressed` #6E9A68, `ink` #4B4038, `inkSoft` #93826F, `pressed` #FFF7E9, `startLabel` #FFFDF7; radii `buttonRadius` 22, `cardRadius` 26, `startRadius` 26, `sceneThumbRadius` 16; the Start shadow and card shadow above; and named `TextStyle` getters for wordmark (36/700), tagline (13/500 inkSoft), section label (16/700 ink), preset number (30/700 ink), preset unit (12/600 inkSoft), start label (21/700 startLabel), start suffix (15/600 startLabel @0.85). Use the platform-default font family for now — Plan 05 adds `fontFamily: 'Baloo 2'`/`'Quicksand'` once fonts are bundled; leave a `///` note to that effect. Do not introduce the destructive #E0805F color (reserved for Phase 4 per UI-SPEC).
    Create lib/screens/setup_screen.dart as a `StatefulWidget` taking `initialDurationMin` (default 5). Render, on `AppTokens.bg`: the "Zual" wordmark + tagline header, a "How long?" section label, a 3-column grid of the five preset buttons (1/5/10/15/30) each a `#FFFCF6` 22px-radius card showing the number (30/700) + "min" (12/600), and a fixed-footer Start button (`AppTokens.accent`, 26px radius, "Start · {N} min" two text runs where N is the selected `durationMin`). Hold `durationMin` in local state; tapping a preset sets it and draws the 3px `AppTokens.accent` selection ring as a `Positioned.fill` `IgnorePointer` overlay with matching radius. Start's `onPressed` calls `context.read<TimerController>().start(durationMin)` then `Navigator.of(context).push(MaterialPageRoute(builder: (_) => const PlaceholderRunningScreen()))` — reference PlaceholderRunningScreen (created in Task 3). Leave a clearly-labeled "Pick a scene" placeholder region (empty sized slot) that Plan 02 fills; do not build scene cards here. Extract per-button build helpers so no single build method exceeds ~30 lines (CONVENTIONS.md). Every public class gets a `///` rationale doc (Phase 1 doc-density convention).
    Modify lib/main.dart: make `Future<void> main() async` (keep `WidgetsFlutterBinding.ensureInitialized()`), give `MyApp` an `initialDurationMin` param (default 5) passed into `SetupScreen`, set `home: SetupScreen(initialDurationMin: initialDurationMin)`, and delete `MyHomePage`. Keep the existing `ChangeNotifierProvider<TimerController>.value` + `TimerLifecycleBinder` wiring untouched. (Plan 04 will convert main() to preload SharedPreferences before runApp.)
    Update test/widget_test.dart: remove the "Hello, World!" assertion (that scaffold is gone); pump `MyApp(...)` and assert the "Zual" wordmark renders, so the pre-existing suite stays green.
    Do NOT modify anything under lib/timer/ — Start is a one-line call into the existing public API (RESEARCH Architectural Responsibility Map).
  </action>
  <verify>
    <automated>cd D:/Projects/zual && flutter test test/widget_test.dart && flutter analyze lib/theme/app_tokens.dart lib/screens/setup_screen.dart lib/main.dart</automated>
  </verify>
  <acceptance_criteria>
    - AppTokens exposes the exact hex/radius/shadow constants above; no #E0805F present.
    - SetupScreen renders the five presets with the 5-min default selected and a 3px #7FA87A ring on the selected card; Start label reads "Start · {N} min" reflecting the current selection.
    - main.dart shows SetupScreen as home, MyHomePage is deleted, and test/widget_test.dart passes asserting the "Zual" wordmark (no Hello World).
    - `flutter analyze` reports no errors for the changed files; lib/timer/ is unmodified.
  </acceptance_criteria>
  <done>The app boots into a working duration-picker Setup screen and Start is wired to TimerController.start; the legacy scaffold and its test assertion are removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PlaceholderRunningScreen + Start navigation makes the E2E test green</name>
  <files>lib/screens/placeholder_running_screen.dart, test/screens/setup_screen_test.dart</files>
  <read_first>
    - .planning/phases/02-setup-screen/02-UI-SPEC.md ("Placeholder Running Screen Contract (D-01 – D-04)")
    - lib/timer/timer_controller.dart (phase/progress getters, endTimer())
    - lib/timer/timer_phase.dart (TimerPhase.done)
    - .planning/phases/02-setup-screen/02-CONTEXT.md (D-01..D-04 placeholder decisions)
  </read_first>
  <behavior>
    - A back-control tap calls TimerController.endTimer() and pops back to Setup.
    - When TimerController.phase becomes TimerPhase.done, the screen auto-pops back to Setup.
    - The screen renders zero text/number widgets.
  </behavior>
  <action>
    Create lib/screens/placeholder_running_screen.dart as a `StatefulWidget` that consumes `TimerController` via `context.watch`. Per D-01..D-04 and the UI-SPEC contract: full-bleed `AppTokens.bg` background; a single flat `AppTokens.accent` circle, 300px diameter at remaining==1, scaled by `Transform.scale(scale: remaining)` where `remaining = 1 - controller.progress` — no track ring, no shadow, no color-zone lerp, zero text/numbers. A small always-visible top-left back control using `Icons.arrow_back` (`AppTokens.ink`, 44x44 min tap target) wrapped in `Semantics(label: 'End timer and return to setup')`; its `onPressed` calls `context.read<TimerController>().endTimer()` then `Navigator.of(context).pop()` immediately with no confirmation dialog (D-02). When `controller.phase == TimerPhase.done`, schedule a single post-frame `Navigator.pop()` back to Setup (D-04) — guard with a `bool _popped` so it fires once. No long-press recognizer, no bottom sheet, no pause/resume (D-03).
    Then extend test/screens/setup_screen_test.dart with the placeholder cases so the file goes GREEN: after Start, tapping the back control (find by the Semantics label) returns to Setup and leaves the controller in `TimerPhase.setup`; and advancing the injected clock past the duration + calling `controller.syncToWallClock()` drives `TimerPhase.done` and auto-pops to Setup.
    Do NOT modify lib/timer/.
  </action>
  <verify>
    <automated>cd D:/Projects/zual && flutter test test/screens/setup_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - PlaceholderRunningScreen renders a shrinking accent circle driven by progress, with no text/number widgets anywhere.
    - Back control (semantic label "End timer and return to setup") calls endTimer() and pops with no dialog; reaching TimerPhase.done auto-pops once.
    - test/screens/setup_screen_test.dart is fully GREEN (SETUP-01, SETUP-04, default, back-return, done-auto-return).
  </acceptance_criteria>
  <done>The walking slice is closed end-to-end: pick duration -> Start -> placeholder countdown -> return, with the E2E test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parent tap -> app state | Only trusted local UI input; no network, no external/untrusted data enters this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01P | Denial of Service | Start -> TimerController.start(minutes) | low | accept | Minutes originates from a fixed preset set (1/5/10/15/30); TimerController.start already clamps to 1..120 (Phase 1). No untrusted value path. |
| T-02-02P | Tampering | Preset selection state | low | accept | Pure in-memory local widget state; no persistence or external input in this plan (persistence hardening is Plan 04's threat model). |
</threat_model>

<verification>
- `flutter test test/screens/setup_screen_test.dart` and `flutter test test/widget_test.dart` both green.
- `flutter analyze` clean for changed files; nothing under lib/timer/ modified (`git diff --name-only` shows no timer/ paths).
</verification>

<success_criteria>
- SETUP-01: presets 1/5/10/15/30 selectable with visible selection ring; 5-min default.
- SETUP-04: Start calls TimerController.start(selectedMinutes) and navigates to the running placeholder.
- App home is SetupScreen; Hello World scaffold and its test assertion are gone.
</success_criteria>

<output>
Create `.planning/phases/02-setup-screen/02-01-SUMMARY.md` when done.
</output>

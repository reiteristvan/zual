---
phase: 03-scene-themes
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - lib/scenes/sunrise/sunrise_painter.dart
  - lib/scenes/sunrise/sunrise_scene.dart
  - lib/scenes/scene_registry.dart
  - test/scenes/sunrise/sunrise_painter_test.dart
  - test/scenes/sunrise/sunrise_scene_test.dart
autonomous: true
requirements: [SCENE-02]
must_haves:
  truths:
    - "Selecting Night to Sunrise and pressing Start shows a full-screen sky interpolating night→day as time passes."
    - "Stars and the moon fade out (clamped, never crashing) and the sun rises to its highest point; the hill silhouette warms from #26314F to #6E9060."
    - "Stars twinkle on a gentle staggered loop driven by the same per-scene ticker as progress — no second AnimationController."
    - "The scene renders without throwing across the full 0..1 progress sweep, including past p≈0.435 (stars) and p≈0.588 (moon) where the raw fade formulas go negative."
    - "D-03: 'smooth, no jank' perceptual validation of the sunrise scene is an end-of-phase human check on a real low/mid-end Android device (API 24–28); automated widget tests cover progress-driven correctness as the CI-checkable layer."
  artifacts:
    - lib/scenes/sunrise/sunrise_painter.dart
    - lib/scenes/sunrise/sunrise_scene.dart
  key_links:
    - "scene_registry.sceneFor maps SceneTheme.sunrise to SunriseScene (replacing the Plan 01 pending fallback)."
    - "SunrisePainter.shouldRepaint compares both progress and the twinkle loop phase (Pitfall 2)."
    - "Every progress-derived opacity/alpha is .clamp(0.0,1.0)'d before withValues(alpha:) (Pitfall 3)."
---

<objective>
Deliver the Night to Sunrise scene: a progress-driven gradient sky, 28 twinkling stars, a fading moon, a rising glowing sun, and a warming hill silhouette — all as a pure function of TimerController.progress, replacing the interim pending fallback for this theme.

Purpose: The second real scene, exercising the gradient-per-frame and clamped-opacity paths that the disc scene did not.
Output: SunrisePainter (+ pure fns starOpacity/moonOpacity/sunTopFraction/hillColor), SunriseScene, and the sunrise arm of scene_registry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scene-themes/03-CONTEXT.md
@.planning/phases/03-scene-themes/03-RESEARCH.md
@.planning/phases/03-scene-themes/03-PATTERNS.md
@.planning/phases/03-scene-themes/03-UI-SPEC.md
@lib/scenes/scene_renderer.dart
@lib/scenes/scene_registry.dart
</context>

## Phase Goal

**As a** young child waiting for a timer, **I want to** watch a full-screen wordless scene that visibly changes as time passes, **so that** I can tell from across the room roughly how much longer I have to wait — without reading any numbers or words.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sunrise pure formulas + SunrisePainter</name>
  <files>lib/scenes/sunrise/sunrise_painter.dart, test/scenes/sunrise/sunrise_painter_test.dart</files>
  <read_first>
    - .planning/phases/03-scene-themes/03-RESEARCH.md (Code Examples §"Sunrise formulas (clamped)"; Pitfall 3 negative-opacity clamp; Pitfall 5 gradient-recreation caching)
    - .planning/phases/03-scene-themes/03-UI-SPEC.md (§"D. Night to Sunrise" Color + Geometry tables; Decorative Loop Contract twinkle row; End-of-run visual correctness for sunrise)
    - .planning/phases/03-scene-themes/03-PATTERNS.md (sunrise_painter.dart pattern assignment; withValues(alpha:) + gradient-via-shader + shouldRepaint shared patterns)
    - lib/scenes/scene_preview.dart (SunrisePreviewPainter: LinearGradient/RadialGradient shader usage, withValues(alpha:) convention)
    - lib/scenes/scene_renderer.dart (loopPhase(Duration) helper the scene passes into the painter for twinkle)
  </read_first>
  <behavior>
    - starOpacity(0.0)==1.0; starOpacity(0.435)≈0.0 boundary; starOpacity(0.6)==0.0 (clamped, not negative); starOpacity is always within 0..1 across a 0.0→1.0 sweep.
    - moonOpacity(0.0)==1.0; moonOpacity(0.588)≈0.0 boundary; moonOpacity(0.9)==0.0 (clamped); always within 0..1.
    - sunTopFraction(0.0)==0.86; sunTopFraction(1.0)==0.22.
    - hillColor(0.0)==const Color(0xFF26314F); hillColor(1.0)==const Color(0xFF6E9060).
    - SunrisePainter.shouldRepaint(old) is true when progress or twinkle phase changed, false when neither did.
  </behavior>
  <action>
    Create sunrise_painter.dart with these top-level pure functions exactly per RESEARCH Code Examples (locked D-01, clamps are the required Pitfall-3 correction): `double starOpacity(double p) => (1 - p*2.3).clamp(0.0,1.0);`, `double moonOpacity(double p) => (1 - p*1.7).clamp(0.0,1.0);`, `double sunTopFraction(double p) => (86 - p*64)/100;`, `Color hillColor(double p) => Color.lerp(const Color(0xFF26314F), const Color(0xFF6E9060), p)!;`.

    Create `SunrisePainter extends CustomPainter` taking `progress` and a `twinklePhase` (0..1 from the scene's loopPhase) as constructor fields. In paint, per §D (all values logical px or fractions of size as noted; % values computed against size.width/size.height at paint time):
    - Sky: full-bleed rect filled with a top→bottom LinearGradient whose top stop is Color.lerp(Color(0xFF182449), Color(0xFF8FC9EA), progress) and bottom stop is Color.lerp(Color(0xFF3A2F5C), Color(0xFFFFDBA6), progress). Reconstruct this shader each frame (colors are progress-driven — cannot cache) per Pitfall 5.
    - Stars (28, i in 0..27): 4×4px white dots at left=((i*37)%100)% of width, top=(4+((i*53)%44))% of height. Each star's alpha = base × twinkle × starOpacity(progress), where base = 0.55 + ((i*13)%40)/90, twinkle oscillates 0.35→1→0.35 on a 3s loop staggered per star by (i%6)*0.5s (derive from twinklePhase + per-star offset), and the layer-wide progress fade is starOpacity(progress). Clamp the final combined alpha to 0..1 before withValues(alpha:).
    - Moon: 72×72px circle at top 12%, right 18%; alpha = moonOpacity(progress) (clamped); color Color(0xFFEDE7D6) with its soft glow.
    - Sun: 130×130px circle centered at left 50%, top = sunTopFraction(progress) of height; radial gradient Color(0xFFFFE9AE)→Color(0xFFF3AE44); glow blur = 40 + progress*90, spread = 12 + progress*46, glow color = Color.fromRGBO(247,193,91, (0.3 + progress*0.5).clamp(0.0,1.0)) — use this exact Zual.dc.html glow-growth formula verbatim (UI-SPEC §D).
    - Hill: very wide flat rounded silhouette, width 150% of screen (left -25%, right -25%), height 210px, bottom -70px, color hillColor(progress).
    Cache progress-independent Paint objects as fields (Pitfall 5). Implement shouldRepaint comparing both progress and twinklePhase (Pitfall 2). Do not add any CSS-style ease/transition.

    Write sunrise_painter_test.dart covering all pure-function behaviors above, plus a table-style sweep asserting starOpacity/moonOpacity stay within 0..1 for progress in {0.0,0.2,0.435,0.5,0.588,0.75,1.0}.
  </action>
  <verify>
    <automated>flutter test test/scenes/sunrise/sunrise_painter_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/scenes/sunrise/sunrise_painter_test.dart` passes all <behavior> assertions.
    - Sweep test confirms starOpacity and moonOpacity return values within [0.0,1.0] at every sampled progress, including past 0.435 and 0.588.
    - `flutter analyze` reports no new errors in lib/scenes/sunrise/sunrise_painter.dart.
  </acceptance_criteria>
  <done>All four sunrise formulas are unit-tested pure functions with the required clamps; SunrisePainter draws the progress-driven sky/stars/moon/sun/hill and repaints correctly on progress or twinkle change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SunriseScene + register sunrise in the scene registry</name>
  <files>lib/scenes/sunrise/sunrise_scene.dart, lib/scenes/scene_registry.dart, test/scenes/sunrise/sunrise_scene_test.dart</files>
  <read_first>
    - lib/scenes/scene_renderer.dart (SceneRendererState base: progress getter, loopPhase, ticker start/stop)
    - lib/scenes/disc/disc_scene.dart (the DiscScene shape to mirror for SunriseScene)
    - lib/scenes/scene_registry.dart (current sceneFor switch with the Plan 01 pending fallback for sunrise)
    - test/support/progress_sweep.dart (sweep helper for the widget test)
    - .planning/phases/03-scene-themes/03-UI-SPEC.md (Interaction Contract: no text, nothing tappable)
  </read_first>
  <behavior>
    - SunriseScene pumped across progress 0.0→1.0 renders without throwing at every sample, including past p=0.435 and p=0.588 (guards the negative-opacity pitfall in a full widget context).
    - A mounted SunriseScene subtree contains no Text/numerals and no gesture-reactive ancestor.
    - sceneFor(SceneTheme.sunrise) returns a SunriseScene (no longer the pending fallback).
  </behavior>
  <action>
    Create sunrise_scene.dart mirroring DiscScene: `SunriseScene extends SceneRenderer`, state extends `SceneRendererState<SunriseScene>`, build returns a full-bleed CustomPaint(painter: SunrisePainter(progress: progress, twinklePhase: loopPhase(const Duration(seconds: 3)))). No Scaffold/GestureDetector/Text.

    In scene_registry.dart, replace the sunrise pending arm so `sceneFor` returns `SunriseScene()` for SceneTheme.sunrise; leave walk/car on the pending fallback (Plan 03 finishes them). Import sunrise_scene.dart.

    Write sunrise_scene_test.dart using the progress_sweep helper for the full 0..1 sweep-renders-without-throwing and the no-text/no-gesture assertions; update/extend the registry expectation for sunrise (either here or by extending scene_registry_test.dart) to assert sceneFor(sunrise) is a SunriseScene.
  </action>
  <verify>
    <automated>flutter test test/scenes/sunrise/ test/scenes/scene_registry_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/scenes/sunrise/sunrise_scene_test.dart` passes: full 0.0→1.0 sweep renders without throwing; subtree has zero Text and zero gesture-reactive ancestors.
    - `flutter test test/scenes/scene_registry_test.dart` passes with sceneFor(sunrise) returning a SunriseScene.
    - `flutter test` (full suite) is green.
    - `flutter analyze` reports no new errors.
  </acceptance_criteria>
  <done>Selecting Night to Sunrise and pressing Start shows the real animated sunrise scene; it never throws across the full run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TimerController.progress → sunrise formulas | Progress feeds opacity/position math whose raw forms go out of range; the sole input crossing into draw code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Denial of Service | starOpacity/moonOpacity + combined star alpha | high | mitigate | .clamp(0.0,1.0) every progress-derived opacity/alpha before withValues(alpha:); the raw formulas go negative past p≈0.435/0.588 and would otherwise assert-crash the render loop mid-run. Guarded by the full-sweep widget test. |
| T-03-04 | Denial of Service | Sun glow alpha (0.3 + p*0.5) | low | mitigate | .clamp(0.0,1.0) the glow alpha before use so it cannot exceed 1.0 near p=1. |

T-03-03 is severity=high (a crash mid-countdown makes the app unusable) but is fully mitigated in-plan by mandatory clamping plus the 0..1 sweep test — it does not remain open, so the block-on-high threshold is satisfied.
</threat_model>

<verification>
- `flutter test test/scenes/sunrise/ test/scenes/scene_registry_test.dart` green.
- `flutter test` full suite green.
- `flutter analyze` no new errors.
- End-of-phase human check (D-03): sunrise sky/stars/sun animate smoothly with no jank on a low/mid-end Android device (recorded as a UAT item at the phase gate).
</verification>

<success_criteria>
- Night to Sunrise renders full-screen; sky interpolates night→day, stars/moon fade, sun rises, hill warms, all driven only by progress (SCENE-02).
- No out-of-range opacity ever reaches the Color API; the scene never crashes across a full run.
</success_criteria>

## Artifacts this phase produces (Plan 02)

New symbols/files created by this plan:
- `lib/scenes/sunrise/sunrise_painter.dart`: top-level `double starOpacity(double)`, `double moonOpacity(double)`, `double sunTopFraction(double)`, `Color hillColor(double)`; `SunrisePainter extends CustomPainter`.
- `lib/scenes/sunrise/sunrise_scene.dart`: `SunriseScene extends SceneRenderer`.
- Modified: `lib/scenes/scene_registry.dart` (sunrise arm now returns `SunriseScene()`).

<output>
Create `.planning/phases/03-scene-themes/03-02-SUMMARY.md` when done.
</output>

---
phase: 03-scene-themes
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - lib/scenes/car/car_painter.dart
  - test/scenes/car/car_painter_test.dart
autonomous: true
gap_closure: true
requirements: [SCENE-04]
must_haves:
  truths:
    - "The car's wheels visibly spin (0.7s loop) while the timer is running: CarPainter draws an asymmetric spoke marking (color #6B5E58, the already-locked tire color) from wheel center to rim edge AFTER canvas.rotate(spinAngle), so the rendered raster genuinely differs across spinAngle values (SCENE-04)."
    - "A pixel-diff regression test renders CarPainter at two different spinAngle values (0.0 and pi/2) at the same progress and asserts the rawRgba byte buffers are NOT equal — this test fails on the old two-concentric-circles implementation and passes only once an asymmetric marking is present (closes the shouldRepaint-only blind spot from CR-01 / Truth #8)."
  artifacts:
    - lib/scenes/car/car_painter.dart
    - test/scenes/car/car_painter_test.dart
  key_links:
    - "CarPainter.spinAngle → _paintWheel → canvas.rotate(spinAngle) then canvas.drawLine(Offset.zero, Offset(0, -radius), _wheelSpokePaint) → asymmetric marking → rendered pixels change every frame."
    - "The pixel-diff test in car_painter_test.dart samples the full-scene raster at two spinAngles and asserts inequality — the automated guard the original shouldRepaint-only suite lacked."
---

<objective>
Close the single blocking Phase 3 verification gap (Truth #8 / SCENE-04, code-review CR-01): the Car on a Road scene's wheel-spin decorative loop is a visual no-op because `CarPainter._paintWheel` rotates two perfectly concentric, unmarked circles — a rotationally symmetric shape whose raster is identical at every angle. Add a minimal, user-approved asymmetric marking so the 0.7s spin is actually observable, and add a pixel-diff regression test that would have caught this (the existing suite only asserted `shouldRepaint`'s boolean, never rendered output).

Purpose: A child watching the Car on a Road scene must actually see the wheels turn — the phase's own `done` criterion ("arriving at time-up with spinning wheels"), `03-UI-SPEC.md`'s Decorative Loop Contract (`spin`: "Continuous rotation, 0°→360°"), and the `CarPainter` class doc all require it.
Output: A `_wheelSpokePaint` field + a spoke `drawLine` in `_paintWheel` on `car_painter.dart`; a raster-diff regression test in `car_painter_test.dart`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scene-themes/03-UI-SPEC.md
@.planning/phases/03-scene-themes/03-VERIFICATION.md
@.planning/phases/03-scene-themes/03-REVIEW.md
@lib/scenes/car/car_painter.dart
</context>

## Phase Goal

**As a** young child waiting for a timer, **I want to** watch the car's wheels visibly turn as it drives toward the house, **so that** the scene reads as alive and moving rather than a frozen picture — reinforcing that time is passing.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add asymmetric wheel marking (CR-01 fix) + raster-diff regression test</name>
  <files>lib/scenes/car/car_painter.dart, test/scenes/car/car_painter_test.dart</files>
  <read_first>
    - lib/scenes/car/car_painter.dart (current `_paintWheel` at lines 242-249; the `_wheelRimPaint` Color(0xFF3A3230) and `_wheelTirePaint` Color(0xFF6B5E58) fields in the constructor; `_wheelDiameter = 28` const; the paint-caching / Pitfall-5 convention used for every other Paint field)
    - lib/scenes/car/car_scene.dart (confirms `spinAngle: 2 * pi * loopPhase(const Duration(milliseconds: 700))` is already wired every frame — no change needed here, do NOT modify this file)
    - test/scenes/car/car_painter_test.dart (existing arrival + shouldRepaint groups to preserve; new group is appended here)
    - test/scenes/car/car_scene_test.dart (existing 0..1 sweep + no-text/no-gesture assertions to preserve; do NOT modify this file)
    - .planning/phases/03-scene-themes/03-UI-SPEC.md (§"Decorative Loop Contract" `spin` row; §"F. Car on a Road" wheel geometry: 28×28 circle, 6px tire ring, rim #3A3230, tire #6B5E58 — the two locked colors the marking must reuse)
    - .planning/phases/03-scene-themes/03-VERIFICATION.md (Truth #8 failure reason + `missing` item)
    - .planning/phases/03-scene-themes/03-REVIEW.md (CR-01, including the confirmation that no test asserts on rendered pixels)
  </read_first>
  <behavior>
    - Rendering the full CarPainter scene (fixed progress) to a raster at spinAngle 0.0 versus pi/2 produces DIFFERENT rawRgba byte buffers (the pre-fix two-concentric-circles code produced byte-identical buffers — this is the assertion that would have caught CR-01).
    - The marking uses only the two already-locked wheel colors; no new color (hex) is introduced anywhere in car_painter.dart.
    - The existing arrival-mechanic tests (arrivalLeftFraction 0.06 / 0.68) and the three shouldRepaint tests still pass unchanged.
    - CarScene's existing 0..1 progress sweep and no-text/no-gesture tests still pass unchanged.
  </behavior>
  <action>
    Deliberate, user-approved minimal deviation from the literal prototype: the design source (`design/Zual.dc.html:236-237`) renders each running-scene wheel as a plain `border-radius:50%` circle animated by CSS `@keyframes spin{to{transform:rotate(360deg);}}` — a rotationally symmetric shape, so the spin is invisible in the source too. The user chose to add a minimal asymmetric marking (a spoke/valve-stem line) reusing ONLY the two locked wheel colors — no new color. Document this in a one-line code comment above the new draw call as a deliberate, user-approved fidelity deviation.

    In `lib/scenes/car/car_painter.dart`:
    - Add one new cached Paint field named `_wheelSpokePaint`, initialized in the constructor initializer list alongside `_wheelRimPaint` / `_wheelTirePaint`: color `Color(0xFF6B5E58)` (reuse the already-locked tire color — chosen because it contrasts against the dark `#3A3230` rim disc the spoke is drawn over; introduces no new color), `style = PaintingStyle.stroke`, `strokeWidth = 2`, `strokeCap = StrokeCap.round`.
    - In `_paintWheel`, after the two existing `drawCircle` calls and still inside the `save()`/`rotate(spinAngle)`/`restore()` block, add exactly one line: `canvas.drawLine(Offset.zero, Offset(0, -(_wheelDiameter / 2 - 3)), _wheelSpokePaint);` — a spoke from the wheel center to the top of the rim disc (radius `_wheelDiameter / 2 - 3` = 11px, matching the rim/tire draw radius). Because it is drawn after `canvas.rotate(spinAngle)`, its rendered position sweeps with the angle, making the rotation visible.
    - Do not change `spinAngle`, `shouldRepaint`, the arrival math, or any other geometry. Keep the Pitfall-5 caching convention (the new Paint is a field, built once per painter instance, never re-allocated in `paint`).

    In `test/scenes/car/car_painter_test.dart`:
    - Add `import 'dart:ui' as ui;` and `import 'dart:math';` (for `pi`).
    - Call `TestWidgetsFlutterBinding.ensureInitialized();` at the top of `main()` (required so `Picture.toImage` / `Image.toByteData` work under the test rasterizer).
    - Add a new `group('CarPainter wheel spin is visible (CR-01 / Truth #8 regression)')` with an async test that: for a given spinAngle, records `CarPainter(progress: 0.5, spinAngle: <angle>).paint(canvas, const Size(200, 400))` into a `ui.PictureRecorder`-backed `Canvas`, ends recording, `toImage(200, 400)`, then `toByteData(format: ui.ImageByteFormat.rawRgba)` and returns the bytes as a `Uint8List`. Render at spinAngle `0.0` and at `pi / 2`, and assert the two byte lists are NOT equal (e.g. via `listEquals(a, b)` from `package:flutter/foundation.dart` returning `false`, or an explicit byte scan). Add a short comment stating this assertion fails on the pre-fix concentric-circles code and passes only with an asymmetric marking present.
    - Do NOT delete or weaken the existing arrival-mechanic group or the shouldRepaint group.
  </action>
  <verify>
    <automated>flutter test test/scenes/car/car_painter_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/scenes/car/car_painter_test.dart` passes, including the new raster-diff test asserting the rawRgba buffers from `CarPainter(progress: 0.5, spinAngle: 0.0)` and `CarPainter(progress: 0.5, spinAngle: pi/2)` rendered at `Size(200, 400)` are NOT equal.
    - `car_painter.dart` contains a field named `_wheelSpokePaint` with color `Color(0xFF6B5E58)` and a single `canvas.drawLine(Offset.zero, Offset(0, -(_wheelDiameter / 2 - 3)), _wheelSpokePaint)` inside `_paintWheel` after the two `drawCircle` calls and before `canvas.restore()`.
    - No hex color other than the pre-existing set appears in `car_painter.dart` (the spoke reuses `0xFF6B5E58`).
    - `flutter test test/scenes/car/` passes (car_scene_test.dart unchanged and still green).
    - `flutter test` (full suite) is green.
    - `flutter analyze lib/scenes/car/car_painter.dart` reports no new issues.
  </acceptance_criteria>
  <done>Rendering the Car on a Road scene at two different spinAngle values produces visibly different rasters (asserted by an automated pixel-diff test), so the wheels visibly spin during a running countdown — restoring Truth #8 / SCENE-04 and closing CR-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `spinAngle` (double, from the per-scene ticker) → `_paintWheel` draw math | The only value crossing into the changed code. It is a bounded rotation angle derived internally from wall-clock time (`2*pi*loopPhase(...)`), never user- or network-supplied. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Tampering | `CarPainter._paintWheel` spoke marking | low | accept | Presentation-only change to a pure `CustomPainter`. No new I/O, network, storage, permission, package, or state surface is introduced; the marking is a single bounded `drawLine` inside an existing `save/rotate/restore` block. No attack surface is added, so no mitigation beyond the existing render-loop bounds is required. |
| T-03-08 | Denial of Service | raster-diff test (`toImage`/`toByteData`) | low | accept | Test-only code path at a small fixed `Size(200, 400)`; runs on the CPU test rasterizer, bounded and deterministic. No runtime/production impact. |

This is a presentation-only rendering fix scoped entirely to a pure painter and its test. ASVS L1: no high-severity threats; no network/auth/persistence/new-dependency surface is touched (consistent with the wider Phase 3 threat model — RESEARCH Security Domain).
</threat_model>

<verification>
- `flutter test test/scenes/car/car_painter_test.dart` green, including the new raster-diff regression test (buffers at spinAngle 0.0 vs pi/2 differ).
- `flutter test test/scenes/car/` green — the untouched `car_scene_test.dart` 0..1 sweep and no-text/no-gesture assertions still pass.
- `flutter test` full suite green.
- `flutter analyze lib/scenes/car/car_painter.dart` reports no new issues.
- Optional (documentation-sync only, non-blocking — do NOT expand task scope): `.planning/REQUIREMENTS.md` line 31 / line 96 still show SCENE-04 as unchecked/"Pending"; these may be ticked to reflect actual completion once this plan lands, but that is a doc edit outside this plan's code scope.
- End-of-phase human check (D-03, unchanged): watch the Car on a Road scene run a full countdown on a real low/mid-end Android device (API 24–28) and confirm the wheels visibly rotate smoothly — the perceptual confirmation the automated raster-diff cannot fully substitute for.
</verification>

<success_criteria>
- The Car on a Road scene's wheels visibly rotate during a running countdown (SCENE-04 / Truth #8 restored).
- The marking reuses only the two locked wheel colors (#3A3230 / #6B5E58) — no new color, honoring the fidelity constraint.
- An automated pixel-diff test guards the behavior: it fails on the symmetric two-circle implementation and passes only with the asymmetric marking present.
- No other Phase 3 file or behavior is changed (03-01/02/03 plans untouched; car_scene.dart and car_scene_test.dart unchanged).
</success_criteria>

## Artifacts this phase produces (Plan 04)

New / modified symbols created by this plan:
- `lib/scenes/car/car_painter.dart`:
  - New field `Paint _wheelSpokePaint` (color `Color(0xFF6B5E58)`, stroke, strokeWidth 2, round cap) — cached once per painter instance per Pitfall 5.
  - New draw call inside `_paintWheel`: `canvas.drawLine(Offset.zero, Offset(0, -(_wheelDiameter / 2 - 3)), _wheelSpokePaint)` after the two `drawCircle` calls, inside the `rotate(spinAngle)` block.
- `test/scenes/car/car_painter_test.dart`:
  - New `group('CarPainter wheel spin is visible (CR-01 / Truth #8 regression)')` with an async raster-diff test (`ui.PictureRecorder` → `toImage(200, 400)` → `toByteData(rawRgba)`; asserts buffers at spinAngle 0.0 vs pi/2 differ).

<output>
Create `.planning/phases/03-scene-themes/03-04-SUMMARY.md` when done.
</output>

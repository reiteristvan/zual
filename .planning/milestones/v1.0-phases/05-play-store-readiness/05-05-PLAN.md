---
phase: 05-play-store-readiness
plan: 05
type: execute
wave: 3
depends_on: [05-01, 05-04]
files_modified:
  - screenshots/
autonomous: false
requirements: [PUBLISH-01, PUBLISH-02]

must_haves:
  truths:
    - "The signed release build installs on a real Android device and runs a full countdown to completion without crashing."
    - "The real Night-to-Sunrise launcher icon appears on the device home screen / launcher."
    - "Four full-bleed screenshots exist, one per scene (Shrinking Disc, Night to Sunrise, Walking Home, Car on a Road), with no device frame and no caption overlay."
  artifacts:
    - "screenshots/ directory containing 4 full-bleed PNG screenshots, one per scene"
  key_links:
    - "signed release .aab/.apk (05-01) + adaptive icon (05-04) -> install on device -> full countdown + launcher icon visible"
    - "each of the 4 running scenes -> adb screencap / Android Studio -> screenshots/*.png -> Play Console listing"
---

<objective>
Prove the phase's headline success criterion end-to-end and capture the last listing asset: a
signed release build (plan 05-01) carrying the real adaptive icon (plan 05-04) installs and
runs a full countdown on a real Android device (ROADMAP Success Criterion #4), and four
full-bleed per-scene screenshots (D-11/D-12/D-13) are captured live from that running app for
the Play Console listing. These are inherently human/device steps — no automated test can drive
a real device install or a real screenshot, and D-12 explicitly forbids a staged
screenshot-harness screen.

Purpose: PUBLISH-01 + PUBLISH-02 — final on-device proof of the signed build plus the
screenshot listing assets.
Output: verified on-device release run and a `screenshots/` directory of 4 full-bleed PNGs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-play-store-readiness/05-CONTEXT.md
@.planning/phases/05-play-store-readiness/05-RESEARCH.md
</context>

<mvp_slice>
The final integration slice: after this plan a real user (a parent) can install the actual
signed Zual app on a device, see its real launcher icon, and run a complete countdown — the
whole point of the phase, verified on real hardware — and the developer holds the last asset
(screenshots) needed to fill the Play Console listing.
</mvp_slice>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: On-device signed-release install + full countdown verification</name>
  <action>Pause execution and wait for the developer to build/install the signed release on a
  real Android device and confirm the real launcher icon plus a full countdown to completion,
  per the steps below. Resume only after "approved". If a device is unavailable, this is a hard
  human/hardware dependency (RESEARCH Environment Availability) — surface it rather than routing
  around it.</action>
  <what-built>A signed release bundle (plan 05-01) with the real Night-to-Sunrise adaptive icon
  (plan 05-04). This must be proven on real hardware — device install, launcher-icon appearance,
  and a full countdown to completion cannot be asserted by any automated test.</what-built>
  <how-to-verify>
    1. Build and install the release build on a connected real Android device:
       `flutter build apk --release` then `flutter install --release` (or
       `adb install -r build/app/outputs/flutter-apk/app-release.apk`). An app bundle can't be
       installed directly; the release APK is fine for this on-device proof (the .aab from
       05-01 remains the Play Console upload artifact).
    2. On the device home screen / app drawer, confirm the launcher shows the Night-to-Sunrise
       icon (sunrise gradient background + sun), NOT the default Flutter icon, and the label
       reads "Zual".
    3. Launch the app, set a short duration (e.g. 1 minute) and any scene, press Start, and let
       it run all the way to the finished state. Confirm: countdown runs smoothly, reaches the
       "All done" finished state, no crash, no debug banner.
    4. Type "approved" if install succeeds, the real icon appears, and a full countdown
       completes cleanly; otherwise describe what failed.
  </how-to-verify>
  <resume-signal>Type "approved" (signed release installs, real icon shows, full countdown completes on device) or describe the issue.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Capture 4 full-bleed per-scene screenshots from the running app</name>
  <action>Pause execution and wait for the developer to capture four full-bleed screenshots
  (one per scene) from the real running app into a screenshots/ directory, per the steps below.
  Do not build a staged screenshot-harness screen (forbidden by D-12) or synthesize images.
  Resume only after "approved".</action>
  <what-built>The app runs all four scenes normally. Play Console needs 4 full-bleed
  screenshots (one per scene) captured from the real running app — device capture is outside
  any in-repo test's reach, and D-12 forbids a staged capture screen.</what-built>
  <how-to-verify>
    1. Create a `screenshots/` directory at the repo root.
    2. For EACH of the 4 scenes (Shrinking Disc, Night to Sunrise, Walking Home, Car on a Road):
       start a timer with that scene, let it reach a representative progress point that best
       shows the scene's character (roughly ~40% elapsed is a good default; your call per scene),
       and capture the full screen via `adb exec-out screencap -p > screenshots/<scene>.png` or
       Android Studio's screenshot tool.
    3. Confirm each PNG is full-bleed (no phone device frame, no added caption/text overlay per
       D-13), portrait, and shows the intended scene.
    4. Result: `screenshots/` contains exactly 4 PNGs, one clearly per scene.
    5. Type "approved" once all 4 full-bleed screenshots exist.
  </how-to-verify>
  <resume-signal>Type "approved" (4 full-bleed per-scene screenshots saved under screenshots/) or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan performs on-device verification and screenshot capture; it adds no code, no
dependency, no network surface, and no persisted app state. Screenshots contain only the
wordless, data-free scene visuals (no PII — the app displays no text, numbers, or user data).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08 | Information Disclosure | committed screenshots | low | accept | Screenshots show only wordless scene graphics with zero user/personal data (the app renders no text or numbers by design); safe to commit for listing use. Accepted. |
</threat_model>

<verification>
- Human confirms the signed release installs, shows the real adaptive icon, and completes a
  full countdown on a real device (Task 1 — ROADMAP Success Criterion #4).
- `screenshots/` holds 4 full-bleed per-scene PNGs (Task 2).
- `flutter test` (full existing suite) stays green — this plan adds no app code.
</verification>

<success_criteria>
- A signed release build runs a full countdown on a real Android device with the real launcher
  icon (PUBLISH-01 + PUBLISH-02, ROADMAP Success Criterion #4).
- Four full-bleed per-scene screenshots exist for the Play Store listing (PUBLISH-02; D-11/12/13).
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 05-05)

- `screenshots/` — NEW directory with 4 full-bleed per-scene PNG screenshots (Shrinking Disc, Night to Sunrise, Walking Home, Car on a Road).
- On-device verification record (in SUMMARY): signed release install + real icon + full countdown confirmed on real hardware.
</artifacts>

<output>
Create `.planning/phases/05-play-store-readiness/05-05-SUMMARY.md` when done.
</output>

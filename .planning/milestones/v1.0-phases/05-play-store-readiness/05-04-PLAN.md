---
phase: 05-play-store-readiness
plan: 04
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - test/tool/icon_painters.dart
  - test/tool/generate_launcher_icon_test.dart
  - assets/icon/icon_foreground.png
  - assets/icon/icon_background.png
  - pubspec.yaml
  - android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml
autonomous: true
requirements: [PUBLISH-02]

must_haves:
  truths:
    - "The Android launcher shows a Night-to-Sunrise-motif adaptive icon, not Flutter's default icon."
    - "The icon foreground is one big simple shape with generous padding that survives circle/squircle masking and stays legible at 48dp."
    - "The icon background is a scene-matched sunrise gradient, not the flat warm-cream UI token and not a flat hex color."
    - "The adaptive icon set (foreground/background layers + legacy per-density fallback) is generated, not hand-placed."
  artifacts:
    - "assets/icon/icon_foreground.png — big simple sun silhouette, generated"
    - "assets/icon/icon_background.png — sunrise gradient, generated"
    - "android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml — adaptive icon descriptor"
    - "pubspec.yaml flutter_launcher_icons dev dependency + config block"
  key_links:
    - "SunrisePainter palette -> icon painters -> renderPainterToPng -> assets/icon/*.png -> flutter_launcher_icons -> mipmap-anydpi-v26/ic_launcher.xml + legacy mipmap-*/ic_launcher.png"
---

<objective>
Generate the app's real adaptive launcher icon programmatically from the Night to Sunrise
motif (D-01, D-04), replacing Flutter's default icon (PUBLISH-02). Reuse the proven
`renderPainterToPng` helper (plan 05-02) to render two source PNGs — a scene-matched sunrise
gradient background (D-02) and a big, simple, well-padded sun silhouette foreground (D-03) —
then let `flutter_launcher_icons` turn them into the full Android adaptive-icon asset set.

Gate: this plan depends on plan 05-02's spike PASSING. If 05-02's SUMMARY reports the
programmatic render path FAILED, do not proceed programmatically — instead capture the icon
source from a live-device screenshot of SunriseScene at a fixed progress (Assumption A1
fallback) and feed those PNGs to flutter_launcher_icons; the rest of this plan is unchanged.

Purpose: PUBLISH-02 — a publishable app needs a real, on-brand launcher icon.
Output: `assets/icon/icon_foreground.png`, `assets/icon/icon_background.png`, a
`flutter_launcher_icons` config + dev dependency, and the generated
`mipmap-anydpi-v26/ic_launcher.xml` + legacy density fallbacks.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-play-store-readiness/05-CONTEXT.md
@.planning/phases/05-play-store-readiness/05-RESEARCH.md
@lib/scenes/sunrise/sunrise_painter.dart
@test/tool/icon_renderer.dart
</context>

<mvp_slice>
Extends the icon slice past the 05-02 proving spike into a real, user-visible outcome: after
this plan the app installs with a recognizable Zual sunrise icon in the launcher — a capability
that did not exist before. It reuses the render helper (no new render risk) and adds only the
icon-specific painters plus the standard flutter_launcher_icons pipeline.
</mvp_slice>

<tasks>

<task type="auto">
  <name>Task 1: Render the sunrise-motif foreground + background source PNGs</name>
  <files>test/tool/icon_painters.dart, test/tool/generate_launcher_icon_test.dart, assets/icon/icon_foreground.png, assets/icon/icon_background.png</files>
  <read_first>
    - .planning/phases/05-play-store-readiness/05-02-SUMMARY.md (did the programmatic render path pass or fall back? — GATES this task's approach)
    - test/tool/icon_renderer.dart (renderPainterToPng(CustomPainter, Size) helper from plan 05-02)
    - lib/scenes/sunrise/sunrise_painter.dart (scene palette hexes to reuse: sky top 0xFF182449->0xFF8FC9EA, sky bottom 0xFF3A2F5C->0xFFFFDBA6, sun gradient 0xFFFFE9AE->0xFFF3AE44, moon 0xFFEDE7D6)
    - .planning/phases/05-play-store-readiness/05-CONTEXT.md (D-01 sunrise motif, D-02 scene-matched gradient bg, D-03 big/simple/padded foreground, D-04 programmatic-in-Dart)
    - .planning/phases/05-play-store-readiness/05-RESEARCH.md (Pattern 1/2, Open Question 1: export at 1024x1024)
  </read_first>
  <action>
    Create test/tool/icon_painters.dart with two small CustomPainters (reuse the SunrisePainter
    palette hexes above — those constants are private in sunrise_painter.dart, so restate the
    same hex values here with a comment noting they are the scene-matched Night-to-Sunrise
    palette per D-02):
    (a) IconBackgroundPainter — fills the canvas with a vertical LinearGradient reproducing a
    bright-sunrise moment of the scene sky (top lerped toward 0xFF8FC9EA, bottom toward
    0xFFFFDBA6, i.e. the scene's high-progress sky), giving the scene-matched gradient D-02
    requires (NOT the flat warm-cream UI token, NOT a flat hex).
    (b) IconForegroundPainter — one dominant, simple shape: a large sun disc using the scene's
    RadialGradient sun colors (0xFFFFE9AE center -> 0xFFF3AE44 edge), centered, sized to leave
    generous padding inside the adaptive safe zone (draw the sun radius at roughly 30-34% of the
    canvas width so it survives circle/squircle masking and reads at 48dp per D-03); optionally
    a soft glow and a single simple hill arc at the very bottom for silhouette character, but
    keep detail minimal — no stars, no moon, no fine scene fidelity. Transparent elsewhere so
    the background layer shows through.
    In test/tool/generate_launcher_icon_test.dart, add a testWidgets case that, inside
    `tester.runAsync`, calls renderPainterToPng for each painter at Size(1024,1024) (Open
    Question 1) and writes `assets/icon/icon_background.png` and `assets/icon/icon_foreground.png`
    (create the assets/icon dir with recursive:true). Assert both files exist, exceed 1000 bytes,
    and start with the PNG signature. If 05-02 reported a FAILED spike, skip the programmatic
    render and instead document + place device-screenshot-sourced PNGs at the same two paths
    (Assumption A1 fallback), then continue to Task 2.
  </action>
  <verify>
    <automated>cd "D:/Projects/zual" && flutter test test/tool/generate_launcher_icon_test.dart && test -f assets/icon/icon_foreground.png && test -f assets/icon/icon_background.png</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/tool/generate_launcher_icon_test.dart` passes.
    - `assets/icon/icon_foreground.png` and `assets/icon/icon_background.png` both exist, are > 1000 bytes, and begin with the PNG signature (asserted in the test).
    - `test/tool/icon_painters.dart` defines IconBackgroundPainter (gradient fill) and IconForegroundPainter (single dominant sun shape, padded, transparent surround) — source assertion.
    - The background painter uses a multi-stop/lerped gradient, not a single flat color (source assertion — LinearGradient with distinct top/bottom colors).
  </acceptance_criteria>
  <done>Two source PNGs — a scene-matched sunrise gradient background and a big simple padded sun foreground — are generated at 1024x1024 from the Night-to-Sunrise palette, ready for flutter_launcher_icons.</done>
</task>

<task type="auto">
  <name>Task 2: Wire flutter_launcher_icons and generate the adaptive icon set</name>
  <files>pubspec.yaml, android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml, android/app/src/main/res/mipmap-*/ic_launcher.png</files>
  <read_first>
    - pubspec.yaml (current dev_dependencies; note plan 05-01 already edited version/description — re-read current state before editing)
    - assets/icon/icon_foreground.png, assets/icon/icon_background.png (from Task 1)
    - .planning/phases/05-play-store-readiness/05-RESEARCH.md (Pattern 4: flutter_launcher_icons config with PNG-path gradient background; Standard Stack: flutter_launcher_icons ^0.14.4, min_sdk_android 24; Package Legitimacy Audit: Approved)
    - android/app/src/main/res/ (existing mipmap-*/ic_launcher.png default icons to be regenerated)
  </read_first>
  <action>
    Add `flutter_launcher_icons: ^0.14.4` to pubspec.yaml dev_dependencies (this pub package is
    already legitimacy-audited Approved in RESEARCH — no install checkpoint needed). Add a
    top-level `flutter_launcher_icons:` config block with: `android: "launcher_icon"`,
    `min_sdk_android: 24` (matches this project's minSdk), `image_path:
    "assets/icon/icon_foreground.png"` (legacy fallback source), `adaptive_icon_foreground:
    "assets/icon/icon_foreground.png"`, and `adaptive_icon_background:
    "assets/icon/icon_background.png"` — the PNG-PATH form of the background is required because
    D-02's background is a gradient, which a single hex value cannot express (do NOT use a flat
    hex `adaptive_icon_background`). Run `flutter pub get` then `dart run flutter_launcher_icons`.
    Confirm it generated `android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml` (the
    adaptive descriptor pointing at the foreground/background layers) and regenerated the legacy
    per-density `mipmap-*/ic_launcher.png` fallbacks. Do not hand-edit the generated XML/PNGs.
    Leave AndroidManifest's `android:icon="@mipmap/ic_launcher"` reference as-is (already correct).
  </action>
  <verify>
    <automated>cd "D:/Projects/zual" && grep -q "flutter_launcher_icons:" pubspec.yaml && grep -q 'adaptive_icon_background: "assets/icon/icon_background.png"' pubspec.yaml && test -f android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml && grep -qi "foreground" android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml && flutter build apk --debug</automated>
  </verify>
  <acceptance_criteria>
    - `pubspec.yaml` dev_dependencies includes `flutter_launcher_icons: ^0.14.4`.
    - `pubspec.yaml` has a `flutter_launcher_icons:` config block with `adaptive_icon_foreground` and `adaptive_icon_background` both pointing at the generated PNG paths (background is the PNG-path form, not a flat hex).
    - `android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml` exists and references a foreground layer.
    - Legacy `mipmap-*/ic_launcher.png` fallbacks were regenerated (no longer the Flutter default).
    - `flutter build apk --debug` succeeds with the new icon resources.
  </acceptance_criteria>
  <done>flutter_launcher_icons has produced the full Android adaptive-icon set from the two generated sunrise PNGs; the app builds with a real Night-to-Sunrise launcher icon replacing Flutter's default.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan adds one dev-only pub dependency and generates icon assets. No runtime trust
boundary, network surface, user input, or persisted app state is introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | flutter_launcher_icons dev dependency (supply chain) | medium | mitigate | Pinned to `^0.14.4`; RESEARCH Package Legitimacy Audit verified it against the pub.dev registry (fluttercommunity org, first published 2017, source repo confirmed) with verdict Approved — not [ASSUMED]/[SUS], so no blocking-human install checkpoint is required. Dev-only (not shipped in the app binary). |
| T-05-07 | Tampering | generated icon assets overwriting res/mipmap-* | low | accept | Generation is via the standard `dart run flutter_launcher_icons` tool writing only icon resources; no source/logic files touched. Accepted. |
</threat_model>

<verification>
- `flutter test test/tool/generate_launcher_icon_test.dart` passes (source PNGs generated).
- `dart run flutter_launcher_icons` produces `mipmap-anydpi-v26/ic_launcher.xml` + legacy fallbacks.
- `flutter build apk --debug` succeeds with the new icon resources.
- `flutter test` (full existing suite) stays green.
</verification>

<success_criteria>
- The app's Android launcher icon is a Night-to-Sunrise adaptive icon (scene-matched gradient
  background + big simple padded sun foreground), generated programmatically, replacing the
  Flutter default (PUBLISH-02 icon requirement; D-01 through D-04).
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 05-04)

- `test/tool/icon_painters.dart` — NEW: `IconBackgroundPainter` (sunrise gradient) and `IconForegroundPainter` (big simple padded sun).
- `test/tool/generate_launcher_icon_test.dart` — EXTENDED: renders and writes the two icon source PNGs.
- `assets/icon/icon_foreground.png`, `assets/icon/icon_background.png` — NEW generated icon sources (1024x1024).
- `pubspec.yaml` — NEW: `flutter_launcher_icons: ^0.14.4` dev dependency + `flutter_launcher_icons:` config block.
- `android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml` — NEW adaptive icon descriptor (generated).
- `android/app/src/main/res/mipmap-*/ic_launcher.png` — regenerated legacy density fallbacks (generated).
</artifacts>

<output>
Create `.planning/phases/05-play-store-readiness/05-04-SUMMARY.md` when done.
</output>
import 'dart:typed_data';

import 'package:flutter/rendering.dart';

/// Renders a [CustomPainter] headlessly to PNG bytes.
///
/// Stub for the RED phase of the Task 1 TDD cycle (05-02-PLAN.md) --
/// intentionally throws so the accompanying spike test fails before the
/// real dart:ui rendering pipeline is implemented.
Future<Uint8List> renderPainterToPng(CustomPainter painter, Size size) {
  throw UnimplementedError('renderPainterToPng not yet implemented');
}

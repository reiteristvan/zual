<!-- GSD:project-start source:PROJECT.md -->

## Project

**Zual**

Zual is a visual countdown timer for children roughly ages 2–6 who don't yet understand
minutes and hours. A parent sets a duration and picks a scene; the child watches a
full-screen, wordless, number-free visualization that makes time remaining readable at a
glance from across a room. When time is up, a soft chime plays and the visual settles into
a calm finished state. It is deliberately not a productivity tool — playful but calm, suitable
for a bedroom or kitchen counter.

**Core Value:** A child with no concept of clock time can look at the screen from across a room and understand,
without any numbers or words, roughly how much longer they have to wait.

### Constraints

- **Tech stack**: Flutter — build on the existing scaffold rather than switching frameworks, despite the design doc's suggestion of React Native/Expo or SwiftUI for greenfield
- **Platform**: Android only for v1 — existing scaffold also supports web, but web is out of scope for this milestone
- **Fidelity**: Visual/behavioral fidelity to `design/README.md` is high-priority — colors, radii, timings, and interaction thresholds (e.g. 850ms long-press) are treated as final, not starting points
- **Distribution**: Intended for eventual Play Store publication — needs to reach a publishable, polished state, not just a personal prototype

<!-- GSD:project-end -->

<!-- GSD:stack-start source:codebase/STACK.md -->

## Technology Stack

## Languages

- Dart 3.10.7+ - Application logic and UI components
- HTML/CSS/JavaScript - Web platform support (generated by Flutter)
- Kotlin - Android platform support (generated by Flutter)

## Runtime

- Flutter 3.18.0-18.0.pre.54 or later
- Dart SDK (bundled with Flutter)
- Pub (Dart package manager)
- Lockfile: `pubspec.lock` (present)

## Frameworks

- Flutter 3.18.0+ - UI framework for cross-platform applications
- Material Design 3 - UI design system (uses Material Icons and theme system)
- flutter_test - Widget and unit testing framework
- flutter_lints 6.0.0 - Dart analyzer rules and linting configuration

## Key Dependencies

- flutter - SDK dependency for all Flutter functionality
- flutter_test - Required for testing widgets and functionality
- cupertino_icons 1.0.9 - iOS-style icon font (available but app uses Material Design)
- async 2.13.1 - Async utilities
- collection 1.19.1 - Collection utilities
- meta 1.17.0 - Annotations and metadata utilities
- vector_math 2.2.0 - Math utilities for graphics
- material_color_utilities 0.11.1 - Material color utilities
- test_api 0.7.7 - Test assertion library
- matcher 0.12.17 - Matcher library for assertions
- leak_tracker 11.0.2 - Memory leak detection in tests
- leak_tracker_flutter_testing 3.0.10 - Flutter-specific leak tracking

## Configuration

- No environment variables required
- No .env file configured
- `pubspec.yaml` - Project manifest with dependencies and metadata
- `analysis_options.yaml` - Dart analyzer and linter configuration
- `pubspec.lock` - Locked dependency versions
- `android/gradle.properties` - Android Gradle configuration

## Platform Requirements

- Flutter SDK (3.18.0-18.0.pre.54+)
- Dart SDK (3.10.7+, included with Flutter)
- Platform-specific requirements:
- **Android:** Android 16+ (implied by Flutter requirements)
- **Web:** Any modern browser supporting WebGL
- **iOS:** iOS 11.0+ (configured but not in active platforms based on .metadata)
- Android (primary target)
- Web (primary target)
- iOS (configuration present but not actively built)

<!-- GSD:stack-end -->

<!-- GSD:conventions-start source:CONVENTIONS.md -->

## Conventions

## Naming Patterns

- `.dart` extension for all Dart source files
- Snake_case for file names: `main.dart`, `widget_test.dart`
- Entry file: `main.dart` in `lib/` directory
- PascalCase for all class names: `MyApp`, `MyHomePage`
- Widget classes should suffix descriptors: prefer `MyAppWidget`, `MyHomePage` for widget types
- State classes use `_MyAppState` pattern when splitting State from StatefulWidget
- camelCase for all function names: `main()`, `build()`, `runApp()`
- Avoid leading underscore unless function is private to file scope
- Private functions use `_functionName` convention
- camelCase for variable and parameter names: `context`, `tester`, `seedColor`
- Constants use camelCase in Dart (not UPPER_SNAKE_CASE): `myConstant`
- Const keyword used for compile-time constants: `const MyApp()`, `const Text('Hello')`
- Use full type annotations: `Widget build(BuildContext context)`
- Avoid `var` when type is not obvious; prefer explicit types
- Use `final` for variables that won't be reassigned
- Use `const` for widget construction when possible for performance

## Code Style

- Tool: Dart formatter (integrated in Flutter SDK, activated via `flutter format`)
- Indentation: 2 spaces (Flutter standard)
- Line length: 80 characters (configurable, Flutter recommends)
- Spacing: 1 blank line between methods, 2 blank lines between classes
- Tool: `flutter_lints` 6.0.0 via `package:flutter_lints/flutter.yaml`
- Activated in: `analysis_options.yaml`
- Run with: `flutter analyze`
- Rules can be disabled per-file with `// ignore_for_file: rule_name` or per-line with `// ignore: rule_name`
- Default rules focus on Flutter best practices, null safety, and code quality

## Import Organization

- Not currently configured in this project
- When configuring, use `.packages` or define in `pubspec.yaml` under `environment:` section
- Use `package:` imports for consistency, not relative imports in production code
- Only use relative imports when necessary in test files
- Group imports by category with blank line separation

## Error Handling

- Currently minimal error handling in scaffold code
- Use try-catch for exception handling: `try { } catch (e) { }`
- Dart uses null safety by default (non-nullable by default)
- Use `?.` operator for safe navigation: `obj?.method()`
- Use `??` operator for null coalescing: `value ?? defaultValue`
- Use `late` keyword for late initialization of non-nullable variables

## Logging

- Avoid `print()` statements in production (flutter_lints includes `avoid_print` rule)
- Use `debugPrint()` for debug output: `debugPrint('Message')`
- No centralized logging service configured yet
- Consider `logging` package for structured logging in future versions

## Comments

- Comments should explain WHY, not WHAT
- Avoid obvious comments: `// increment counter` is unnecessary for `counter++;`
- Comment complex logic, non-obvious performance decisions, or workarounds
- Keep comments concise and up-to-date with code
- Not yet applied to existing code (main.dart is scaffold-generated)
- Use `///` for public API documentation: 
- Use `//` for regular comments, `///` for documentation
- Document public classes, methods, and properties
- Document parameters with `@param` and return values with `@return`

## Function Design

- Keep functions small and focused (under 50 lines preferred)
- Extract helpers when build methods exceed 30 lines
- Single Responsibility Principle: one purpose per function
- Use named parameters in constructors for widgets: `MyApp({super.key})`
- Prefer `super.key` over `key: key` in constructor delegation
- Use required keyword for mandatory parameters
- Group related parameters together
- Explicit return types: `Widget build(...)` not just `build(...)`
- Use `void` when function has no return value
- Async functions return `Future<T>`: `Future<void>`, `Future<String>`
- Always return from all code paths (use `return;` for void if needed)

## Module Design

- Create barrel files for easy imports: `export 'widgets/my_widget.dart';`
- Prefer explicit exports over re-exporting entire files
- Keep public API minimal and intentional
- Group related functionality in subdirectories under `lib/`
- Common structure: `lib/widgets/`, `lib/screens/`, `lib/models/`, `lib/services/`
- Tests mirror source structure: `test/widgets/`, `test/screens/`, etc.

<!-- GSD:conventions-end -->

<!-- GSD:architecture-start source:ARCHITECTURE.md -->

## Architecture

## System Overview

```text

```

## Component Responsibilities

| Component | Responsibility | File |
|-----------|----------------|------|
| **MyApp** | Root widget; Material theme initialization; app entry point | `lib/main.dart` |
| **MyHomePage** | Main UI screen; Scaffold with AppBar and centered text | `lib/main.dart` |
| **Flutter Framework** | Handles widget lifecycle, state, rendering, gesture handling | (Flutter SDK) |
| **Android Platform** | Native Android app wrapper, permissions, lifecycle | `android/app/src/main/` |
| **Web Platform** | HTML/Canvas entry point, browser integration | `web/` |

## Pattern Overview

- Entry point: `main()` function calls `runApp(const MyApp())`
- Root widget: `MyApp` extends `StatelessWidget` for immutability
- Theme: Material Design 3 with `ColorScheme.fromSeed()`
- Platform coverage: Android and Web (iOS configuration omitted)
- Testing: Widget test checks for text presence via `WidgetTester`

## Layers

- Purpose: Render visual interface and handle user interactions
- Location: `lib/main.dart`
- Contains: `MyApp` (root), `MyHomePage` (screen), Material components
- Depends on: Flutter framework, Material Design package
- Used by: Flutter runtime, platform-specific wrappers
- Purpose: Native platform integration and app initialization
- Location: `android/app/src/main/`, `web/`
- Contains: Android manifest, Kotlin main activity, web HTML entry point
- Depends on: Flutter embedding (Android), web JavaScript runtime
- Used by: Operating system / web browser

## Data Flow

### Primary Request Path (App Launch)

### Testing Flow

- No state management library (GetX, Riverpod, Provider) currently used
- All widgets are `StatelessWidget` — no local state
- Theme applied via `ThemeData` and `ColorScheme`

## Key Abstractions

- Purpose: Provide consistent Material Design UI across platforms
- Examples: `MaterialApp`, `Scaffold`, `AppBar`, `Center`
- Pattern: Material Design 3 with deep purple seed color
- Purpose: Declarative UI building
- Examples: `MyApp` (root), `MyHomePage` (screen)
- Pattern: Functional widget composition via `build()` methods

## Entry Points

- Location: `lib/main.dart` (line 3-5: `main()` function)
- Triggers: App launch (all platforms)
- Responsibilities: Initialize `MyApp` widget and pass to Flutter runtime
- Location: `android/app/src/main/AndroidManifest.xml` (lines 6-27)
- Triggers: Android app launch
- Responsibilities: Declare `MainActivity`, set theme, configure activity lifecycle
- Location: `web/index.html` (line 36)
- Triggers: Browser loads page
- Responsibilities: Load Flutter bootstrap and HTML metadata

## Architectural Constraints

- **Platform support:** Android and Web only (iOS not configured)
- **State management:** Stateless design — no global state container
- **Dart version:** Requires Dart SDK 3.10.7+
- **Widget model:** Single-file definition — all code in `main.dart`
- **Theme:** Hardcoded Material Design 3 with deep purple seed
- **Navigation:** No routing/navigation framework (only single screen)

## Anti-Patterns

### All Code in Single File

- `lib/screens/` — Screen widgets
- `lib/widgets/` — Reusable widget components
- `lib/models/` — Data classes
- `lib/services/` — Business logic

### No State Management Layer

- Provider pattern (minimal, Flutter-recommended)
- Riverpod (type-safe evolution of Provider)
- GetX (opinionated, all-in-one)

### Hardcoded Theme

- `lib/theme/app_theme.dart` — Define `lightTheme`, `darkTheme`
- Pass to `MaterialApp.theme` and `MaterialApp.darkTheme`

## Error Handling

- No error boundaries or error handling in widgets
- Flutter default error handling (red error screens in debug)

## Cross-Cutting Concerns

<!-- GSD:architecture-end -->

<!-- GSD:skills-start source:skills/ -->

## Project Skills

No project skills found. Add skills to any of: `.claude/skills/`, `.agents/skills/`, `.cursor/skills/`, `.github/skills/`, or `.codex/skills/` with a `SKILL.md` index file.
<!-- GSD:skills-end -->

<!-- GSD:workflow-start source:GSD defaults -->

## GSD Workflow Enforcement

Before using Edit, Write, or other file-changing tools, start work through a GSD command so planning artifacts and execution context stay in sync.

Use these entry points:

- `/gsd-quick` for small fixes, doc updates, and ad-hoc tasks
- `/gsd-debug` for investigation and bug fixing
- `/gsd-execute-phase` for planned phase work

Do not make direct repo edits outside a GSD workflow unless the user explicitly asks to bypass it.
<!-- GSD:workflow-end -->

<!-- GSD:profile-start -->

## Developer Profile

> Profile not yet configured. Run `/gsd-profile-user` to generate your developer profile.
> This section is managed by `generate-claude-profile` -- do not edit manually.
<!-- GSD:profile-end -->
